# Claude Code compatibility

Follow `SKILL.md` exactly and use `claude-code` as the lifecycle actor.

The Brainstem remains the only product. Do not introduce separate Claude
branding into the user experience, upload private Brainstem state, or change
the Grail installer or kernel. Operate locally, apply only the exact plan hash,
verify the real `/chat` path, and stop on any failed gate.
