{
  "name": "rapp-go",
  "version": "2.1.0",
  "private": true,
  "type": "module",
  "description": "A private moment field where thoughts, pictures, sounds, places, time, and weather evolve one permanent 3D companion.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "serve": "node tests/server.mjs",
    "test": "node --test tests/unit/*.test.js",
    "test:e2e": "playwright test",
    "check": "npm run test && npm run test:e2e && npm run build"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  },
  "dependencies": {
    "three": "^0.185.1"
  }
}
