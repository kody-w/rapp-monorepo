# rapp-brain — your second brain, as a verifiable dimension

One command turns any folder of markdown into an append-only, content-addressed,
tick-anchored knowledge chain — poolable across devices and people with forgeries
bounced by arithmetic. Spec: [SPEC.md](SPEC.md). `python3 brainify.py <dir> <slug>`.
