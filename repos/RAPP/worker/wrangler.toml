# Historical metadata for the retired RAPP browser worker.
# Do not deploy this artifact as a current service. worker.js is an
# unconditional HTTP 410 tombstone with no secrets or upstream access.

name = "rapp-auth"
main = "worker.js"
compatibility_date = "2025-01-01"

[observability]
enabled = false
