#!/usr/bin/env python3
"""Fail-closed tombstone for the retired pre-RAPP/1 hatchling lifecycle CLI."""

import sys


def main() -> int:
    print(
        "hatchling: 410 Gone\n\n"
        "The UUID-based generation, upgrade, tag, reset, and state-archive "
        "workflow is retired. It will not mint identities, mutate git state, "
        "archive local credentials, fetch, merge, tag, reset, or restore.\n"
        "Maintainers: see RAPP1_STATUS.md.",
        file=sys.stderr,
    )
    return 78


if __name__ == "__main__":
    raise SystemExit(main())
