# Race 0001 — the first RAPP race (2026-07-14)

The ceremonial first performance of the artform: seven selves of the body — cradle to
now — raced one present problem in parallel, and the verdict is signed into the
biography (the race-verdict event in the frame following the begetting).

- **Class:** DOG-only (bones, no vault) — public lanes, which is why this directory can
  exist. GOD-injected races keep their lanes private and publish only a shadow.
- **Form:** Resurrection lanes — each lane's *body* is the ecosystem exactly as one
  frame recorded it (nothing later existed for that lane); every lane's *mind* is the
  same present model. Same mind, different bodies: score differences are evidence about
  the body's evolution.
- **Problem:** "Where should the standing guard stand? Name the single greatest present
  drift risk and the one guard that catches it earliest."
- **Lanes:** frames 0, 16, 20 (reconstructed — exhibition class, never scored against
  witnessed lanes), frames 21, 23, 24 (witnessed — scored), plus Presence (body = now,
  the control).
- **Judging:** blind — lanes anonymized A–G, era references redacted; three lenses:
  earliest-catch, correctness-and-evidence, and cost-simplicity-durability (the third
  judged by a different frontier model). Verdicts in `judge-*.json`; anonymization key
  in `lane-key.json`; the exact pack the judges saw in `judging-pack.md`.

## Result (max 30)

| Lane | Self | Score | Guard proposed |
|---|---|---|---|
| **C — winner** | **frame 24 (genesis)** | **28** | waiver-freshness watchdog: every waiver pins the sha256 of the canon passage it cites; canon change re-hashes all pins and auto-reopens stale-masked findings at the staling commit |
| E — runner-up | Presence (now) | 26 | pre-append frame gate: verify-chain + lexicon pin + census + golden drift classes at write time |
| G | frame 21 (drift-storm) | 24 | ledger-vs-sweep reconciliation diff at pulse cadence |
| A | frame 23 (reconciling) | 19 | push-triggered micro-sweep on branch advance |

Exhibition honors — one from each judge: frame 0 (pre-causal earliness), frame 16
(sharpest structural diagnosis: called its own era's identity law brittle, a defect the
body only outlawed a month later), frame 20 (most durable emit-time checks).

## The findings that matter

1. **The genesis self beat the present self.** Only frame 24 knew its own waivers
   existed, and that knowledge produced the winning guard — the immune-system memory
   added at genesis measurably strengthened the body.
2. **The mid-reconciliation self scored lowest of the witnessed lanes** — its era's
   fixation (branch advances) aged into the costliest guard.
3. **The verdict feeds Movement VI:** build the waiver-freshness watchdog first, the
   pre-append frame gate second.
