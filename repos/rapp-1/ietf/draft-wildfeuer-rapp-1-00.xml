<?xml version="1.0" encoding="UTF-8"?>
  <?xml-stylesheet type="text/xsl" href="rfc2629.xslt" ?>
  <!-- generated by https://github.com/cabo/kramdown-rfc version 1.7.43 (Ruby 4.0.6) -->


<!DOCTYPE rfc  [
  <!ENTITY nbsp    "&#160;">
  <!ENTITY zwsp   "&#8203;">
  <!ENTITY nbhy   "&#8209;">
  <!ENTITY wj     "&#8288;">

]>


<rfc ipr="trust200902" docName="draft-wildfeuer-rapp-1-00" category="info" submissionType="independent" tocInclude="true" sortRefs="true" symRefs="true">
  <front>
    <title abbrev="RAPP/1">RAPP/1: The RAPP Protocol Suite</title>

    <author initials="K." surname="Wildfeuer" fullname="Kody Wildfeuer">
      <organization>Wildhaven Homes LLC</organization>
      <address>
        <email>wildhavenhomesllc@gmail.com</email>
      </address>
    </author>

    <date year="2026" month="September" day="01"/>

    <area>Applications</area>
    
    <keyword>agents</keyword> <keyword>content-addressing</keyword> <keyword>canonicalization</keyword> <keyword>JCS</keyword> <keyword>provenance</keyword>

    <abstract>


<?line 43?>

<t>RAPP/1 is a profile over existing standards that lets independent programs exchange
verifiable, content-addressed records about agents: one canonicalization (JCS), one
domain-separated hash, one mint-once identity, one eleven-key event envelope, one wire,
and one package format. Two independent implementations that follow this document
produce byte-identical artifacts with no out-of-band agreement. The normative text of
record is the append-only specification chain published by the author; this document
is a stable, archival rendering of it.</t>



    </abstract>



  </front>

  <middle>


<?line 53?>

<section anchor="introduction"><name>Introduction</name>
<t>RAPP is a content-addressed distributed organism. Its integrity rests on one invariant: <strong>the same
concept has the same bytes everywhere.</strong> This document specifies, normatively and completely, five
load-bearing primitives so any two independent implementations interoperate <strong>byte-for-byte with no
out-of-band agreement</strong>: canonicalization (§4), content addressing (§5), identity (§6), the frame (§7),
the egg (§9) — all riding one wire (§8): <spanx style="verb">POST /chat</spanx>, or a signed append-only frame. Implementations add
agents, cartridges, and registered <spanx style="verb">kind</spanx>s — never new endpoints, never new envelopes.</t>

<t><strong>Repository scope.</strong> <spanx style="verb">kody-w/RAPP</spanx> is the canonical public RAPP foundation,
product home, reference implementation, organism model, and philosophy.
<spanx style="verb">kody-w/rapp-1</spanx> is the canonical interoperable protocol authority. This
specification governs wire-compatible bytes and conformance; it does not absorb
the RAPP product or any downstream Rappter/RapterBox LLC product.</t>

<t><strong>Specification authority.</strong> The append-only DOGG chain at
<spanx style="verb">anchor/chain.jsonl</spanx> carries the normative revision content. Its hashes prove
integrity, not authority or ratification. Until an authenticated RAPP
registry/checkpoint exists, the chain accepted by the owner onto protected
canonical <spanx style="verb">kody-w/rapp-1</spanx> main is authoritative; an immutable accepted commit
plus head frame hash is the portable checkpoint. <spanx style="verb">SPEC.md</spanx> is the byte-exact
materialized human view, and <spanx style="verb">orient.json</spanx> is only a discovery beacon. The
selected chain-frame hash, not a mutable path or <spanx style="verb">rev-N</spanx> label, is the durable
identity of a protocol revision (§12.2).</t>

<section anchor="the-layered-model"><name>The layered model</name>
<t><spanx style="verb">
  L5  EGG        cartridge packaging (§9)          — MIME-multipart analogue
  L4  FRAME      universal event envelope (§7)     — the IP packet of RAPP
  L3  WIRE       transport: /chat + frames (§8)    — HTTP-analogue single method
  L2  IDENTITY   rappid namespace + trust (§6,§10) — URI + PKI analogue
  L1  ADDRESS    canonicalization + hash (§4,§5)   — the git object model
</spanx>
A higher layer <strong>MUST NOT</strong> redefine a lower one. Every layer names exactly one canonical form.</t>

</section>
</section>
<section anchor="requirements-language"><name>Requirements language</name>
<t>The key words <strong>MUST</strong>, <strong>MUST NOT</strong>, <strong>REQUIRED</strong>, <strong>SHALL</strong>, <strong>SHOULD</strong>, <strong>MAY</strong> are as in <xref target="RFC2119"/>/
<xref target="RFC8174"/> when in <strong>bold caps</strong>. (The Federal Constitution adopts the same clause.)</t>

</section>
<section anchor="terminology"><name>Terminology</name>
<t><strong>organism</strong> — a running brainstem with persistent identity. <strong>rappid</strong> — the eternal, mint-once name of an
organism/door/object (§6): minted once from a UUIDv4 or a public key, never derived from content or name.
<strong>frame</strong> — one immutable content-addressed event (§7). <strong>stream</strong> — an append-only hash-chained frame
sequence sharing one <spanx style="verb">stream_id</spanx>. <strong>particle / wave</strong> — a frame's two domain-separated addresses:
payload-hash / whole-frame-hash (§7.3). <strong>canonical form</strong> — the one <xref target="RFC8785"/> byte string for a value
(§4). <strong>legacy form</strong> — any other historical encoding; legacy is drift and <strong>MUST</strong> be migrated out
(Fed. Const. Art. III), except sealed re-genesis history (§12.1).
<strong>Grail kernel</strong> — an estate-declared, byte-pinned runtime substrate. Its <spanx style="verb">grail_id</spanx> is
<spanx style="verb">"grail:" || Hb("rapp/1:grail", kernel_bytes)</spanx>; repository, immutable ref, commit, path, raw SHA-256,
and byte length are provenance and verification data, not alternate identities. Once activated through
§13.3, those bytes are permanent (Protocol Constitution Art. 15); capabilities evolve outside them.
<strong>release capsule</strong> — the canonical <spanx style="verb">rapp-cicd/1-release</spanx> payload identifying one immutable candidate.
<strong>serving lineage</strong> — the sequence of qualified release identities that have received user traffic;
the currently served release is immutable even while a separate candidate lineage grows.
<strong>deployment cell</strong> — an independently observable and isolatable runtime failure domain governed by
<spanx style="verb">rapp-deploy/1</spanx>.</t>

</section>
<section anchor="canonicalization-l1"><name>Canonicalization (L1)</name>
<t><spanx style="verb">canonical(v)</spanx> is the UTF-8 byte string produced by <strong><xref target="RFC8785"/> JCS</strong> for the value <spanx style="verb">v</spanx>, defined <strong>only</strong>
over I-JSON <xref target="RFC7493"/>. JCS fixes member-name ordering (UTF-16 code-unit), string escaping, and number
serialization (ECMAScript <spanx style="verb">Number::toString</spanx>, ECMA-262); there is no insignificant whitespace and no
byte-order mark.</t>

<t><strong>RAPP input-domain profile (parse-side interoperability — this is a RAPP rule, not a JCS mandate).</strong> An
implementation <strong>MUST</strong> refuse (never repair) any JSON value that, at any depth, contains: (a) duplicate
member names in one object; (b) an unpaired UTF-16 surrogate in any string; (c) a number token that does
not survive the binary64 round-trip — let <spanx style="verb">d</spanx> be the token's nearest binary64 value under <spanx style="verb">roundTiesToEven</spanx>
(the IEEE-754 default; ±∞ are admissible results); the token is refused iff <spanx style="verb">d</spanx> is not finite, or the
<xref target="RFC8785"/> serialization of <spanx style="verb">d</spanx> (ECMA-262 <spanx style="verb">Number::toString</spanx>) denotes a different mathematical value than
the token (so <spanx style="verb">0.1</spanx> is accepted — it round-trips — while <spanx style="verb">9007199254740993</spanx> and <spanx style="verb">1e999</spanx> (→ +∞) are refused); (d) canonical form exceeding 1 MiB or JSON nesting depth
exceeding 64 (the root value is depth 1; each nested object/array adds 1). Refusal is whole (§7.5-style),
never partial.</t>

<t><strong>No normalization.</strong> RAPP applies <strong>no</strong> Unicode normalization when hashing, storing, or re-emitting an
existing value; strings are code-point sequences preserved verbatim and equality everywhere is code-point
equality (no canonical-equivalence matching). A producer creating a <strong>new</strong> human-or-identifier string
(slug, kind, label, <spanx style="verb">payload</spanx> object key) <strong>MUST</strong> emit it in Unicode NFC.</t>

<ul empty="true"><li>
  <t><strong>Migration note (drift C4):</strong> the <spanx style="verb">twin</spanx>/<spanx style="verb">rapp-body</spanx> <spanx style="verb">_frame.mjs::canonicalize()</spanx> (sorted-key
<spanx style="verb">JSON.stringify</spanx>) coincides with JCS only for string-only payloads; it <strong>MUST</strong> be replaced by the JCS
implementation of record, imported (not re-typed) by every repo that content-addresses.</t>
</li></ul>

</section>
<section anchor="content-addressing-l1-domain-separated"><name>Content addressing (L1) — domain-separated</name>
<t>Every hash is <strong>domain-separated</strong> (git's <spanx style="verb">type\0</spanx>, Nix's tagged store — collisions across address spaces
are made unconstructible):
<spanx style="verb">
H(space, v) = lowercase_hex( SHA-256( utf8(space) || 0x0A || canonical(v) ) )     ; v a value (§4)
Hb(space, b) = lowercase_hex( SHA-256( utf8(space) || 0x0A || b ) )                ; b raw octets
</spanx>
<spanx style="verb">space</spanx> is an exact ASCII tag, none containing <spanx style="verb">0x0A</spanx>: <spanx style="verb">"rapp/1:particle"</spanx>, <spanx style="verb">"rapp/1:wave"</spanx>, <spanx style="verb">"rapp/1:egg"</spanx>,
<spanx style="verb">"rapp/1:egg-manifest"</spanx>, <spanx style="verb">"rapp/1:rappid"</spanx>, <spanx style="verb">"rapp/1:grail"</spanx>, <spanx style="verb">"rapp/1:seal"</spanx>,
<spanx style="verb">"rapp/1:sealed-aad"</spanx>, <spanx style="verb">"rapp/1:sealed-key-request"</spanx>. A tag is used by either <spanx style="verb">H</spanx> or <spanx style="verb">Hb</spanx>, never
both. Output is always exactly 64 lowercase hex, <strong>never truncated or uppercased</strong>. Two values
are treated as the same object iff their same-space hashes are equal; SHA-256 collision resistance
FIPS 180-4 is a security assumption of this standard (§14). A <spanx style="verb">name/X.Y</spanx> label is never identity — only
a hash is. A bare 64-hex is meaningful <strong>only</strong> within its space; an implementation <strong>MUST NOT</strong> dereference
a hash from one space as an object of another, and content-addressed stores <strong>MUST</strong> key by <spanx style="verb">(space, hash)</spanx>.</t>

</section>
<section anchor="identity-the-rappid-l2"><name>Identity — the rappid (L2)</name>
<t>## Grammar (case-sensitive, <xref target="RFC7405"/>)
<spanx style="verb">abnf
rappid    = %s"rappid:@" owner "/" slug ":" hash
owner     = lclabel                       ; the lowercase GitHub login (1-39 chars)
slug      = lclabel                        ; 1-100 chars
lclabel   = lcalnum *( ["-"] lcalnum )     ; no leading/trailing/adjacent hyphen
lcalnum   = LCALPHA / DIGIT
LCALPHA   = %x61-7A                         ; a-z
hash      = 64HEXDIGLC
HEXDIGLC  = DIGIT / %x61-66                 ; 0-9 a-f
</spanx>
<spanx style="verb">owner</spanx> <strong>MUST</strong> be the lowercase form of the GitHub login (logins are case-insensitive; display casing is
presentation, never identity). Lengths are normative: <spanx style="verb">owner</spanx> 1–39, <spanx style="verb">slug</spanx> 1–100; an implementation
<strong>MUST</strong> refuse longer. This self-locating form is the <strong>only</strong> conformant rappid; <spanx style="verb">rappid:&lt;slug&gt;:&lt;hash&gt;</spanx>,
<spanx style="verb">rappid:v2:…</spanx>, bare UUIDs, <spanx style="verb">moment:</spanx>/name-hash derivations are legacy and <strong>MUST</strong> be migrated out
(Art. III), not read forever.</t>

<section anchor="streamid-and-kind-grammar"><name>stream_id and kind grammar</name>
<t><spanx style="verb">abnf
stream_id   = memory-stream / body-stream / swarm-stream
memory-stream = rappid ":" instance      ; one organism instance's memory
body-stream   = rappid                    ; an organism's biography
swarm-stream  = %s"net:" lclabel          ; a planetary-wire stream
instance      = lclabel                   ; 1-64 chars
kind          = lclabel "." lclabel       ; each label 1-64 chars
</spanx>
A <spanx style="verb">kind</spanx> string carries <strong>no intrinsic family</strong>; the §13 registry binds each registered <spanx style="verb">kind</spanx> to exactly
one family. Membership is tested by exact-match against the registry — never prefix inference, never wildcards.</t>

</section>
<section anchor="minting-mint-once"><name>Minting (mint-once)</name>
<t>The 64-hex tail is minted <strong>exactly once</strong> per identity, then immutable:
- <strong>keyless:</strong> <spanx style="verb">tail = Hb("rapp/1:rappid", uuid4_octets)</spanx>, where <spanx style="verb">uuid4_octets</spanx> is the 16-octet binary
  UUIDv4 <xref target="RFC9562"/> §5.4 (field/byte order per §4 of that RFC).
- <strong>keyed:</strong> <spanx style="verb">tail = Hb("rapp/1:rappid", SPKI_DER)</spanx>, the DER <spanx style="verb">SubjectPublicKeyInfo</spanx> <xref target="RFC5280"/> of the master key.</t>

<t>A producer <strong>MUST NOT</strong> derive the tail from owner/slug or any name (<spanx style="verb">sha256("owner/slug")</spanx> is prohibited —
drift ID-01/C3). On read of an existing <spanx style="verb">rappid.json</spanx> an implementation <strong>MUST</strong> reuse the stored tail
(canonicalize-on-read) and <strong>MUST NOT</strong> re-mint — with exactly one mechanism: the owner-authorized
<strong>re-anchor</strong> (§6.3), which mints a fresh 64-hex tail once per authorization and records the superseded id
in <spanx style="verb">_migrated_from</spanx>. Re-anchor is lawful in exactly three cases: (a) a 128→256-bit provisional upgrade
(§6.3); (b) §10 key rotation or compromise; (c) migrating a pre-rev-3 keyed tail minted with the un-tagged
<spanx style="verb">sha256(SPKI)</spanx> formula (which fails §10 discovery and <strong>MUST</strong> be re-anchored like a provisional identity).</t>

</section>
<section anchor="canonicalization-on-read-provisional-identifiers"><name>Canonicalization on read; provisional identifiers</name>
<t><spanx style="verb">canonicalize_rappid(s)</spanx> restructures any legacy form into §6.1, <strong>preserving the existing hash</strong> (never
inventing one). A restructured identifier whose tail is not exactly 64 lowercase hex (e.g. a legacy 32-hex
tail) is <strong>provisional</strong>: it exists only inside the reading process and <strong>MUST NOT</strong> appear in any emitted
frame, <spanx style="verb">stream_id</spanx>, egg, or registry entry. The one-time owner-authorized 128→256-bit re-anchor mints a
fresh 64-hex, records the old id in <spanx style="verb">_migrated_from</spanx>, and is the only way a provisional identity becomes
usable. A provisional identifier found in a stored artifact is a drift finding (Art. III). Re-anchor is the
single re-mint mechanism (§6.2), lawful only in the enumerated cases: provisional 128→256-bit upgrade,
§10 key rotation/compromise, and pre-rev-3 un-tagged-<spanx style="verb">sha256(SPKI)</spanx> keyed-tail migration.</t>

<t><strong>A re-anchor is valid only with a verifiable authorization</strong> — a self-asserted <spanx style="verb">_migrated_from</spanx> is
insufficient (it would let anyone hijack an identity). A re-anchor <strong>MUST</strong> be recorded as an owner-signed
§13.3 <strong>re-anchor record</strong> <spanx style="verb">{old_rappid, new_rappid, case, utc, sig, old_key_sig?}</spanx>; a consumer <strong>MUST</strong>
refuse a <spanx style="verb">new_rappid</spanx> (and treat <spanx style="verb">_migrated_from</spanx> as drift) unless that record is present and:
- <spanx style="verb">case:"rotation"</spanx> (uncompromised): <spanx style="verb">old_key_sig</spanx> (a §10 JWS by the <strong>old</strong> key) verifies — proof of
  continuity;
- <spanx style="verb">case:"compromise"</spanx>: <spanx style="verb">old_key_sig</spanx> is waived but a §10 <strong>tombstone</strong> for <spanx style="verb">old_rappid</spanx> is registered in the
  same append;
- <spanx style="verb">case:"tag-migrate"</spanx> (pre-rev-3 keyed tail): the verifier checks <spanx style="verb">lowercase_hex(SHA-256(SPKI_DER_old))</spanx> ==
  the old tail;
- <spanx style="verb">case:"upgrade"</spanx> (provisional 128→256): the old provisional id resolved to this owner at read time.
Each mints one fresh tail. The <strong>estate_owner's own</strong> re-anchor record <strong>MUST</strong> be signed by the outgoing
<spanx style="verb">estate_owner</spanx> key (§13.2); root-key compromise is recovered only by out-of-band re-anchoring (§13.1).</t>

</section>
</section>
<section anchor="the-frame-l4"><name>The Frame (L4)</name>
<t>## The envelope — exactly eleven keys
<spanx style="verb">json
{
  "spec":         "rapp/1",
  "kind":         "&lt;klabel.klabel&gt;",
  "stream_id":    "&lt;stream_id&gt;",
  "seq":          &lt;uint53&gt;,
  "utc":          "YYYY-MM-DDTHH:MM:SS.mmmZ",
  "payload":      { },
  "payload_hash": "&lt;64hex&gt;",
  "frame_hash":   "&lt;64hex&gt;",
  "prev":         "&lt;64hex|null&gt;",
  "prev_wave":    "&lt;64hex|null&gt;",
  "sig":          "&lt;jws|null&gt;"
}
</spanx>
- <strong><spanx style="verb">spec</spanx> MUST be the exact string <spanx style="verb">"rapp/1"</spanx></strong> in every frame. <spanx style="verb">rapp-frame/2.0</spanx>/<spanx style="verb">2.1</spanx> are legacy tokens
  and <strong>MUST NOT</strong> be emitted. Any revision changing the key set, any field's grammar, or either hash rule
  <strong>MUST</strong> change this token and land an Art. III total migration; revisions adding only new registered
  <spanx style="verb">kind</spanx>s/registry entries keep the token (Fed. Const. Art. II).
- <strong>Exactly these eleven keys, always present</strong>, none missing, none extra. A field that does not apply is
  present with value <spanx style="verb">null</spanx> (<spanx style="verb">prev</spanx>/<spanx style="verb">prev_wave</spanx> at genesis and on non-swarm streams; <spanx style="verb">sig</spanx> when unsigned) — never omitted,
  because <xref target="RFC8785"/> hashes <spanx style="verb">null</spanx> and an absent key differently. Extra or missing keys are refused (§7.5).
- <strong><spanx style="verb">payload</spanx> MUST be a JSON object</strong> (possibly empty <spanx style="verb">{}</spanx>); never <spanx style="verb">null</spanx>, array, string, number, or bool.
- <spanx style="verb">seq</spanx> is <spanx style="verb">uint53</spanx> (§7.4). A producer <strong>MUST NOT</strong> emit a frame whose canonical form exceeds 1 MiB or
  nesting depth 64 (§4).</t>

<section anchor="kind-families-one-envelope-registry-bound-families"><name>Kind families (one envelope, registry-bound families)</name>
<t>| family | example registered kinds | <spanx style="verb">stream_id</spanx> form | logs |
|---|---|---|---|
| <spanx style="verb">memory</spanx> | <spanx style="verb">memory.chat-turn</spanx>, <spanx style="verb">memory.tool-call</spanx>, <spanx style="verb">memory.save</spanx>, <spanx style="verb">memory.reconstructed</spanx> | memory-stream | one organism's life |
| <spanx style="verb">swarm</spanx>  | <spanx style="verb">swarm.guidance</spanx>, <spanx style="verb">swarm.echo</spanx>, <spanx style="verb">swarm.telemetry</spanx>, <spanx style="verb">swarm.reconstructed</spanx> | swarm-stream | the planetary wire |
| <spanx style="verb">body</spanx>   | <spanx style="verb">body.pulse</spanx>, <spanx style="verb">body.twin-pulse</spanx>, <spanx style="verb">body.reconstructed</spanx>, <spanx style="verb">body.re-genesis</spanx> | body-stream | an organism's biography |
Each family also has a <spanx style="verb">*.re-genesis</spanx> kind (<spanx style="verb">memory.re-genesis</spanx>, <spanx style="verb">swarm.re-genesis</spanx>, <spanx style="verb">body.re-genesis</spanx>)
used only by §12.1. The family is <strong>not</strong> the kind's prefix — it is the §13 registry binding (so
<spanx style="verb">body.twin-pulse</spanx> is family <spanx style="verb">body</spanx>). Adding a family or event is a new registered <spanx style="verb">kind</spanx> on the <strong>same</strong> envelope (Art. IV), never a
new frame type. A frame's <spanx style="verb">kind</spanx> family <strong>MUST</strong> be compatible with its <spanx style="verb">stream_id</spanx> form (table column 3).</t>

</section>
<section anchor="particle-and-wave-the-unification"><name>Particle and wave (the unification)</name>
<t>A frame carries <strong>both</strong> of its domain-separated addresses; a reader collapses it to whichever it needs.
Computed in order:
- <strong>particle</strong> — <spanx style="verb">payload_hash = H("rapp/1:particle", payload)</spanx>. The <strong>worldline identity and chain link</strong>.
- <strong>wave</strong> — <spanx style="verb">frame_hash = H("rapp/1:wave", frame \ {frame_hash, sig})</spanx> — the frame with <strong>exactly</strong> the
  <spanx style="verb">frame_hash</spanx> and <spanx style="verb">sig</spanx> keys removed, all nine remaining keys (including <spanx style="verb">payload_hash</spanx>) present.
Because <spanx style="verb">payload_hash</spanx> is in the wave pre-image, <spanx style="verb">frame_hash</spanx> attests the particle; because only
<spanx style="verb">frame_hash</spanx> (cannot hash itself) and <spanx style="verb">sig</spanx> (signs the result) are removed, the pre-image is unambiguous
and non-circular. Both hashes are always present (never <spanx style="verb">null</spanx>).</t>

</section>
<section anchor="chaining-time-and-merge-order"><name>Chaining, time, and merge order</name>
<t><list style="symbols">
  <t><strong><spanx style="verb">utc</spanx></strong> <strong>MUST</strong> be exactly the 24-byte form <spanx style="verb">YYYY-MM-DDTHH:MM:SS.mmmZ</spanx> — uppercase <spanx style="verb">T</spanx>/<spanx style="verb">Z</spanx>, exactly
three fractional digits, no numeric offset; the seconds field <strong>MUST NOT</strong> be <spanx style="verb">60</spanx> (a leap second clamps
to <spanx style="verb">59.999</spanx>). All <spanx style="verb">utc</spanx> comparisons are <strong>bytewise</strong> over this fixed form (identical to chronological order).</t>
  <t><strong>Worldline chain (particle):</strong> the <strong>genesis</strong> frame has <spanx style="verb">seq</spanx>=0 and <spanx style="verb">prev</spanx>=null; every later frame has
<spanx style="verb">seq</spanx> = predecessor's <spanx style="verb">seq</spanx>+1 (contiguous) and <spanx style="verb">prev</spanx> = predecessor's <spanx style="verb">payload_hash</spanx>. <spanx style="verb">seq</spanx> is <spanx style="verb">uint53</spanx>
(JSON integer, 0 ≤ seq ≤ 2^53−1, no fraction/exponent; a stream nearing 2^53−1 converges by re-genesis).</t>
  <t><strong>Wire chain (wave):</strong> <spanx style="verb">prev_wave</spanx> <strong>MUST</strong> be non-null <strong>iff</strong> <spanx style="verb">stream_id</spanx> is a swarm-stream <strong>and</strong>
<spanx style="verb">seq</spanx> &gt; 0, in which case it equals the predecessor's <spanx style="verb">frame_hash</spanx>; in every other frame (all memory/body
streams, every genesis) it <strong>MUST</strong> be <spanx style="verb">null</spanx>. (Presence is a function of stream family, not transport.)</t>
  <t>A frame is <strong>immutable</strong>: a new state is a new frame at a new hash; the head pointer (§7.6) re-points.</t>
  <t><strong>Cross-stream merge order</strong> (Dream-Catcher) is the total order: ascending <spanx style="verb">utc</spanx> (bytewise), ties broken
by ascending <spanx style="verb">frame_hash</spanx> (bytewise); no further ties are possible (§5).</t>
</list></t>

</section>
<section anchor="verification-the-complete-consumer-checklist"><name>Verification (the complete consumer checklist)</name>
<t>Before accepting a frame, a consumer <strong>MUST</strong>, in order, <strong>refuse</strong> (never repair/reparent) on any failure:
1. <strong>Shape &amp; types:</strong> exactly the eleven §7.1 keys; <spanx style="verb">spec</spanx>==<spanx style="verb">"rapp/1"</spanx>; <spanx style="verb">kind</spanx> a string matching §6.1.1
   ABNF and registered (§13); <spanx style="verb">stream_id</spanx> a string matching §6.1.1; <spanx style="verb">seq</spanx> a <spanx style="verb">uint53</spanx>; <spanx style="verb">utc</spanx> matching the
   §7.4 fixed form <strong>and</strong> a calendar-valid <xref target="RFC3339"/> <spanx style="verb">date-time</spanx> (so <spanx style="verb">2026-13-45T25:61:61.999Z</spanx> is
   refused); <spanx style="verb">payload</spanx> a JSON object; <spanx style="verb">payload_hash</spanx>/<spanx style="verb">frame_hash</spanx> <spanx style="verb">64HEXDIGLC</spanx>; <spanx style="verb">prev</spanx>/<spanx style="verb">prev_wave</spanx> each
   <spanx style="verb">null</spanx> or <spanx style="verb">64HEXDIGLC</spanx>; <spanx style="verb">sig</spanx> <spanx style="verb">null</spanx> or a §10 JWS string.
1a. <strong>Stream binding:</strong> <spanx style="verb">frame.stream_id</spanx> <strong>MUST</strong> byte-equal the identifier of the stream being extended
   or read (its declared <spanx style="verb">stream_id</spanx> at the head pointer / repo path of record). A genesis frame is
   accepted only for the stream it names. (Defeats cross-stream genesis/segment replay — §14.)
2. <strong>Particle:</strong> <spanx style="verb">payload_hash</spanx> == <spanx style="verb">H("rapp/1:particle", payload)</spanx>.
3. <strong>Wave:</strong> <spanx style="verb">frame_hash</spanx> == <spanx style="verb">H("rapp/1:wave", frame \ {frame_hash, sig})</spanx>.
4. <strong>Chain:</strong> <spanx style="verb">seq</spanx>==head.<spanx style="verb">seq</spanx>+1 and <spanx style="verb">prev</spanx>==head.<spanx style="verb">payload_hash</spanx> (or <spanx style="verb">seq</spanx>==0 ∧ <spanx style="verb">prev</spanx>==null at genesis);
   <spanx style="verb">utc</spanx> ≥ head.<spanx style="verb">utc</spanx> (bytewise).
5. <strong>Wire:</strong> if <spanx style="verb">stream_id</spanx> is a swarm-stream and <spanx style="verb">seq</spanx>&gt;0, <spanx style="verb">prev_wave</spanx>==head.<spanx style="verb">frame_hash</spanx>; else
   <spanx style="verb">prev_wave</spanx>==null.
6. <strong>Signature:</strong> if <spanx style="verb">sig</spanx>≠null, verify per §10 (present-but-invalid is refusal); a swarm-stream frame with
   <spanx style="verb">sig</spanx>==null is refused (§8/§10).
Steps 1–5 are <strong>time-independent</strong> (kind lookups only append, §13), so a frame passing them passes forever;
step 6 alone may flip pass→fail when a §10 tombstone with <spanx style="verb">revoked_utc</spanx> ≤ the frame's <spanx style="verb">utc</spanx> is later
registered — the one time-dependent check.</t>

</section>
<section anchor="heads-and-forks"><name>Heads and forks</name>
<t>A stream's <strong>head</strong> is the frame with the greatest verified <spanx style="verb">seq</spanx> descending from its registered genesis
(§13). A consumer <strong>MUST</strong> persist, per stream, the highest <spanx style="verb">(seq, frame_hash)</spanx> it verified and <strong>MUST</strong>
refuse any presented head with lower <spanx style="verb">seq</spanx>, or a different <spanx style="verb">frame_hash</spanx> at an already-verified <spanx style="verb">seq</spanx> (no
rollback, no silent reorg). Two distinct valid frames with equal <spanx style="verb">stream_id</spanx>, <spanx style="verb">seq</spanx>, and <spanx style="verb">prev</spanx> are a
<strong>fork</strong>: a consumer <strong>MUST</strong> refuse both branches past the fork point and surface a drift finding; only
the owner resolves a fork (Art. X), by re-genesis (§12.1) if needed.</t>

<t><strong>Exception (re-genesis head reset).</strong> The no-rollback rule above refuses a lower-<spanx style="verb">seq</spanx> head — but a
legitimate §12.1 re-genesis presents a new genesis at <spanx style="verb">seq</spanx>=0. When (and only when) the §13 registry maps a
<spanx style="verb">stream_id</spanx> to a new genesis <spanx style="verb">frame_hash</spanx> (§12.1 step 3), a consumer <strong>MUST</strong> verify that registered
genesis (§7.5) and then <strong>reset</strong> its persisted head for that <spanx style="verb">stream_id</spanx> to it. Only a registry-published
genesis authorizes a reset; any other lower-<spanx style="verb">seq</spanx> head remains a refused rollback.</t>

</section>
</section>
<section anchor="the-wire-l3"><name>The Wire (L3)</name>
<t>All interaction rides one of exactly two forms:
1. <strong>Synchronous — <spanx style="verb">POST /chat</spanx>, <spanx style="verb">application/json</spanx> both ways.</strong> Request: <spanx style="verb">user_input</spanx> (string, REQUIRED);
   <spanx style="verb">session_id</spanx> (string, OPTIONAL — omit to start a session); <spanx style="verb">idempotency_key</spanx> (string, OPTIONAL — a repeat
   with the same key returns the original response, not a new turn or duplicate session; scoped to
   <spanx style="verb">session_id</spanx> when present, else to the key alone so session-creation is also de-duplicated); unrecognized members <strong>MUST</strong> be
   ignored, never refused. Success: HTTP 200 with <strong>exactly</strong> <spanx style="verb">{response:string, agent_logs:[string],
   session_id:string}</spanx> (no extra members). An unknown <spanx style="verb">session_id</spanx>, a refusal, or a malformed request
   <strong>MUST</strong> be HTTP 422, <spanx style="verb">{error:{code:string, step:string|null}}</spanx> where <spanx style="verb">code</spanx> is a §13-registered error
   code (e.g. <spanx style="verb">"unknown-session"</spanx>) and <spanx style="verb">step</spanx> is the failing §7.5 step as a string — one of
   <spanx style="verb">"1","1a","2","3","4","5","6"</spanx> — or <spanx style="verb">null</spanx>. No other shape is conformant. New capability is a new agent
   behind <spanx style="verb">/chat</spanx>, never a sibling REST route.
2. <strong>Asynchronous — an append-only frame (§7) published to a stream</strong> (a repo path, an <spanx style="verb">events/</spanx> log). A
   frame on a <strong>swarm-stream MUST</strong> carry <spanx style="verb">sig</spanx>≠null (§10); memory/body-stream frames <strong>MAY</strong> be unsigned.
   Any <em>history</em> is safe to read given a trusted head (§14); the hash chain (§5) makes tampering
   detectable.</t>

</section>
<section anchor="the-egg-l5-the-single-egg-spec-of-record"><name>The Egg (L5) — the single egg spec of record</name>
<t>An <strong>egg</strong> is a cartridge packing a unit of the estate. <strong>RAPP §9 is the one egg spec of record</strong> (it
subsumes and retires <spanx style="verb">EGG_FAMILY.md</spanx>, <spanx style="verb">NEIGHBORHOOD_EGG_SPEC.md</spanx>, <spanx style="verb">ESTATE_SPEC.md</spanx>, <spanx style="verb">rappterbook/EGG_SPEC.md</spanx>,
and the rest — drift C7). No other document may re-specify eggs; they cite this section.</t>

<section anchor="container-manifest-and-egg-address"><name>Container, manifest, and egg address</name>
<t>An egg is either a JSON object (<spanx style="verb">invite</spanx>/<spanx style="verb">session</spanx> variants) or a ZIP whose root is <spanx style="verb">manifest.json</spanx> (tree
variants). The manifest is a §4 value with exactly these members:
<spanx style="verb">json
{ "schema": "rapp/1-egg", "variant": "&lt;variant&gt;", "rappid": "&lt;§6.1 rappid&gt;",
  "created_utc": "&lt;§7.4 utc&gt;", "contents": [ {"path":"&lt;rel&gt;","hash":"&lt;64hex&gt;"}, … ], "payload": { },
  "sig": "&lt;jws|null&gt;" }
</spanx>
- <spanx style="verb">contents</spanx> <strong>MUST</strong> list every packed file <strong>except <spanx style="verb">manifest.json</spanx> itself</strong>, exactly once each, with
  <spanx style="verb">hash = Hb("rapp/1:egg", file_octets)</spanx> (§5) over the raw stored octets. <spanx style="verb">contents</spanx> is <strong>always present</strong>;
  for JSON (pointer/session) variants it <strong>MUST</strong> be exactly <spanx style="verb">[]</spanx>.
- <spanx style="verb">path</spanx> <strong>MUST</strong> be a relative POSIX path: <spanx style="verb">/</spanx>-separated NFC UTF-8 segments, no <spanx style="verb">.</spanx>/<spanx style="verb">..</spanx> segment, no
  leading <spanx style="verb">/</spanx>, no backslash, no Windows drive-qualified first segment (<spanx style="verb">ALPHA ":"</spanx>), no duplicate <spanx style="verb">path</spanx>
  in one manifest. A segment <strong>MUST NOT</strong> end in a period/space, contain <spanx style="verb">:</spanx>, contain a C0 control, or have
  a case-insensitive basename equal to <spanx style="verb">CON</spanx>, <spanx style="verb">PRN</spanx>, <spanx style="verb">AUX</spanx>, <spanx style="verb">NUL</spanx>, <spanx style="verb">COM1</spanx>–<spanx style="verb">COM9</spanx>, or <spanx style="verb">LPT1</spanx>–<spanx style="verb">LPT9</spanx>.
  <spanx style="verb">contents</spanx> <strong>MUST</strong> be sorted ascending
  by the UTF-8 bytes of <spanx style="verb">path</spanx>.
- <spanx style="verb">payload</spanx> is a §4 object (variant-specific). <spanx style="verb">sig</spanx> is a §10 JWS over <spanx style="verb">canonical(manifest \ {sig})</spanx>, or
  <spanx style="verb">null</spanx>. For the <spanx style="verb">invite</spanx> variant <spanx style="verb">sig</spanx> is REQUIRED and <strong>MUST</strong> verify with <spanx style="verb">kid</spanx> in the §13.2 estate-owner
  succession (invites are estate-issued; a <spanx style="verb">sig</spanx> by any other key, even a validly registered one, is
  refused — otherwise an attacker mints a fresh rappid and forges invites). For other variants a non-null
  <spanx style="verb">sig</spanx> <strong>MUST</strong> verify per §10 with <spanx style="verb">kid</spanx> == a keyed rappid the consumer resolves via §13; a consumer
  presented a signed egg <strong>MUST</strong> verify it.
- <strong>The egg's one §5 address is</strong> <spanx style="verb">egg_hash = H("rapp/1:egg-manifest", manifest \ {sig})</spanx> — the manifest with
  exactly the <spanx style="verb">sig</spanx> key removed (mirroring §7.3's wave rule: <spanx style="verb">sig</spanx> authenticates the egg, <spanx style="verb">egg_hash</spanx> names
  it, so re-signing never changes identity). Stores key eggs by <spanx style="verb">("rapp/1:egg-manifest", egg_hash)</spanx>.
  (<spanx style="verb">"rapp/1:egg"</spanx> addresses file octets; <spanx style="verb">"rapp/1:egg-manifest"</spanx> addresses the egg as a whole.)
- <strong>Container determinism.</strong> A ZIP variant <strong>MUST</strong> use compression method <spanx style="verb">stored</spanx> (0) for <strong>every</strong> entry
  — no deflate in any variant (deflate is library-dependent, so it cannot be byte-reproducible; transport
  compression, if any, wraps the egg and is not the egg). Entries appear in <spanx style="verb">contents</spanx> order with
  <spanx style="verb">manifest.json</spanx> first; the <spanx style="verb">manifest.json</spanx> entry's octets <strong>MUST</strong> be exactly <spanx style="verb">canonical(manifest)</spanx>; all
  timestamps <spanx style="verb">1980-01-01 00:00:00</spanx>; no extra fields; UTF-8 filename flag set; <spanx style="verb">contents[].hash</spanx> is over the
  file octets (identical to the archive octets under method 0). A JSON-variant egg's serialized form
  <strong>MUST</strong> be exactly <spanx style="verb">canonical(manifest)</spanx>. Two conformant packers of the <strong>same manifest value</strong> thus emit
  byte-identical eggs.</t>

</section>
<section anchor="variants-the-ratified-set-closes-egg-01"><name>Variants (the ratified set — closes EGG-01)</name>
<t>| variant | container | packs | required members |
|---|---|---|---|
| <spanx style="verb">organism</spanx> | ZIP | a full brainstem instance | contents (sorted) MUST include <spanx style="verb">rappid.json</spanx>, <spanx style="verb">soul.md</spanx>; MAY include <spanx style="verb">agents/*</spanx>, <spanx style="verb">organs/*</spanx>, memory files |
| <spanx style="verb">rapplication</spanx> | ZIP | one rapp | contents MUST include <spanx style="verb">rappid.json</spanx> and exactly one <spanx style="verb">agent.py</spanx> at the root (the agent of record); MAY include one <spanx style="verb">ui.html</spanx> and files under <spanx style="verb">state/</spanx> |
| <spanx style="verb">session</spanx> | JSON | one runtime + transcript | <spanx style="verb">payload</spanx> = <spanx style="verb">{runtime:&lt;string&gt;, transcript:[&lt;object&gt;]}</spanx>; contents <spanx style="verb">[]</spanx> |
| <spanx style="verb">invite</spanx> | JSON | a QR-sized pointer (<strong>no packed files</strong>) | <spanx style="verb">payload</spanx> = <spanx style="verb">{target_rappid:&lt;rappid&gt;, target_url:&lt;string&gt;, target_kind:("neighborhood" / "estate")}</spanx>; contents <spanx style="verb">[]</spanx>; <spanx style="verb">sig</spanx> REQUIRED |
| <spanx style="verb">neighborhood</spanx> | ZIP | several organisms meant to live together | <spanx style="verb">payload</spanx> = <spanx style="verb">{members:[&lt;rappid&gt;,…]}</spanx>; contents = one sub-egg per member, named <spanx style="verb">&lt;owner&gt;--&lt;slug&gt;.egg</spanx> at the root, matched by the sub-egg manifest's <spanx style="verb">rappid</spanx> == the <spanx style="verb">payload.members[]</spanx> entry |
| <spanx style="verb">estate</spanx> | ZIP | several neighborhoods | <spanx style="verb">payload</spanx> = <spanx style="verb">{neighborhoods:[&lt;rappid&gt;,…]}</spanx>; contents = one sub-egg per neighborhood, named <spanx style="verb">&lt;owner&gt;--&lt;slug&gt;.egg</spanx> at root, matched by sub-egg <spanx style="verb">rappid</spanx> |
| <spanx style="verb">sealed</spanx> | ZIP | publicly mirrorable ciphertext with scoped key release | contents MUST be exactly <spanx style="verb">ciphertext.bin</spanx>; payload is the closed <spanx style="verb">rapp-sealed-artifact/1</spanx> profile in §9.2.1; <spanx style="verb">sig</spanx> REQUIRED |</t>

<t>The QR-sized invite that caused EGG-01 is the <strong><spanx style="verb">invite</spanx></strong> variant: a signed pointer object, not a
member-packing <spanx style="verb">neighborhood</spanx> egg. The banned legacy stamps (<spanx style="verb">brainstem-egg/2.3-neighborhood</spanx>,
<spanx style="verb">neighborhood-egg/1.0</spanx>) migrate to <spanx style="verb">{schema:"rapp/1-egg", variant:"invite" / "neighborhood"}</spanx> (Art. III).</t>

</section>
<section anchor="sealed-artifact-profile"><name>Sealed artifact profile</name>

<t>The <spanx style="verb">sealed</spanx> variant lets any unauthenticated mirror, including a commit-pinned
<spanx style="verb">raw.githubusercontent.com</spanx> URL, distribute an encrypted artifact globally without distributing its
decryption key.</t>

<t>The public bytes are <strong>ciphertext, not access control</strong>. Bytecode or obfuscation alone provides no
confidentiality. A conformant producer <strong>MUST</strong> use a random 256-bit data-encryption key (DEK) per artifact,
<strong>MUST NOT</strong> embed that DEK or a shared master key in the egg/client/URL, and <strong>MUST NOT</strong> place a password
in a URL. A password, passkey, device assertion, or account credential may authenticate to the key service;
it is not the artifact encryption key.</t>

<t>The ZIP contents are exactly one file:</t>

<t><spanx style="verb">text
ciphertext.bin = AES-256-GCM ciphertext || 16-byte authentication tag
</spanx></t>

<t>The manifest <spanx style="verb">payload</spanx> has exactly:</t>

<t><spanx style="verb">json
{
  "schema": "rapp-sealed-artifact/1",
  "cipher": "A256GCM",
  "nonce": "&lt;12 bytes, unpadded base64url&gt;",
  "plaintext_commitment": "&lt;64hex&gt;",
  "plaintext_bytes": 1234,
  "media_type": "application/wasm",
  "key_id": "&lt;64hex&gt;",
  "key_service_rappid": "rappid:@owner/key-service:64hex",
  "key_service_url": "https://keys.example.com/chat",
  "access": "scoped-key-release",
  "aad_hash": "&lt;64hex&gt;"
}
</spanx></t>

<t><spanx style="verb">plaintext_commitment</spanx> is a keyed commitment that cannot be tested without the DEK:</t>

<t><spanx style="verb">text
prk = HMAC-SHA-256(32 zero octets, DEK)
commitment_key = HMAC-SHA-256(prk, utf8("rapp/1:sealed-commitment") || 0x01)
plaintext_commitment = lowercase_hex(HMAC-SHA-256(commitment_key, plaintext_octets))
</spanx></t>

<t>This is the <xref target="RFC5869"/> HKDF-Extract/HKDF-Expand construction with an empty salt, one 32-byte output block,
and the exact info string <spanx style="verb">rapp/1:sealed-commitment</spanx>. The commitment is public but does not permit an
offline dictionary attack without the random 256-bit DEK.</t>

<t>The exact authenticated-data descriptor is:</t>

<t><spanx style="verb">json
{
  "schema": "rapp-sealed-artifact/1",
  "artifact_rappid": "&lt;manifest.rappid&gt;",
  "created_utc": "&lt;manifest.created_utc&gt;",
  "key_id": "&lt;payload.key_id&gt;",
  "plaintext_commitment": "&lt;payload.plaintext_commitment&gt;",
  "plaintext_bytes": 1234,
  "media_type": "&lt;payload.media_type&gt;"
}
</spanx></t>

<t><spanx style="verb">aad_hash = H("rapp/1:sealed-aad", descriptor)</spanx>, and <spanx style="verb">canonical(descriptor)</spanx> is supplied as AES-GCM
additional authenticated data. <spanx style="verb">nonce</spanx> decodes to exactly 12 bytes. <spanx style="verb">plaintext_bytes</spanx> is an integer from
zero through <spanx style="verb">2^30</spanx> (1 GiB), a RAPP safety ceiling below both the NIST SP 800-38D per-invocation limit
and the deterministic non-ZIP64 container boundary.
<spanx style="verb">ciphertext.bin</spanx> length is exactly <spanx style="verb">plaintext_bytes + 16</spanx>. The sealed egg's manifest signature is REQUIRED
and verifies per §10 with <spanx style="verb">kid</spanx> exactly equal to <spanx style="verb">manifest.rappid</spanx>. A sealed artifact therefore uses a
keyed §6.2 rappid controlled by its authorized publisher; another valid registry key cannot sign on its
behalf.</t>

<t><spanx style="verb">key_service_rappid</spanx> identifies the RAPP organism authorizing key release. <spanx style="verb">key_service_url</spanx> is its absolute
HTTPS §8 <spanx style="verb">POST /chat</spanx> URL and <strong>MUST</strong> end in <spanx style="verb">/chat</spanx>. The URL is location, not authority: the service
response is accepted only when its signatures and identifiers bind to <spanx style="verb">key_service_rappid</spanx>.</t>

<t>The caller sends the following canonical JSON object as the <spanx style="verb">user_input</spanx> string:</t>

<t><spanx style="verb">json
{
  "schema": "rapp-sealed-key-request/1",
  "egg_hash": "&lt;64hex&gt;",
  "key_id": "&lt;64hex&gt;",
  "recipient_rappid": "rappid:@owner/device:64hex",
  "recipient_spki_der_b64": "&lt;standard base64&gt;",
  "request_nonce": "&lt;64hex&gt;",
  "expires_utc": "YYYY-MM-DDTHH:MM:SS.mmmZ",
  "sig": "&lt;recipient detached JWS&gt;"
}
</spanx></t>

<t><spanx style="verb">sig</spanx> is over <spanx style="verb">canonical(request \ {sig})</spanx> and verifies under <spanx style="verb">recipient_rappid</spanx>; <spanx style="verb">egg_hash</spanx> is the §9.1
sealed egg address. The key service verifies entitlement, exact <spanx style="verb">(egg_hash,key_id)</spanx>, request expiry,
recipient key binding, and nonce non-reuse.</t>

<t>The §8 <spanx style="verb">response</spanx> string is canonical JSON:</t>

<t><spanx style="verb">json
{
  "schema": "rapp-sealed-key-release/1",
  "egg_hash": "&lt;request.egg_hash&gt;",
  "key_id": "&lt;request.key_id&gt;",
  "recipient_rappid": "&lt;request.recipient_rappid&gt;",
  "request_hash": "&lt;H('rapp/1:sealed-key-request', request without sig)&gt;",
  "wrap_alg": "ECDH-ES+A256KW",
  "wrapped_key_jwe": "&lt;JWE Compact Serialization&gt;",
  "issued_utc": "YYYY-MM-DDTHH:MM:SS.mmmZ",
  "expires_utc": "YYYY-MM-DDTHH:MM:SS.mmmZ",
  "service_sig": "&lt;key-service detached JWS&gt;"
}
</spanx></t>

<t><spanx style="verb">service_sig</spanx> is over <spanx style="verb">canonical(release \ {service_sig})</spanx> and verifies under <spanx style="verb">key_service_rappid</spanx>.
<spanx style="verb">wrapped_key_jwe</spanx> uses <xref target="RFC7516"/> with <spanx style="verb">alg:"ECDH-ES+A256KW"</spanx> and <spanx style="verb">enc:"A256GCM"</spanx> for the declared
recipient SPKI. The service <strong>MUST NOT</strong> return a global raw DEK. Authorization, expiry, device scope, and
key rotation are service policy recorded in signed frames, not fields that alter the sealed egg's content
address.</t>

<t>A mirror URL <strong>SHOULD</strong> be immutable (for GitHub Raw, pin a commit SHA rather than a moving branch). URL
secrecy is never assumed: integrity comes from the egg address and signature, confidentiality from
AES-GCM, and use authorization from scoped key release.</t>

<t>Revocation prevents future key release. It cannot erase ciphertext or plaintext already downloaded and
decrypted by an authorized recipient. A conformant product <strong>MUST</strong> state this limit plainly. It also
<strong>MUST NOT</strong> claim that encrypted bytecode cannot be inspected after decryption; legal/license controls and
bounded server authority remain separate from cryptographic confidentiality.</t>

</section>
<section anchor="conformance"><name>Conformance</name>
<t><list style="symbols">
  <t><strong>Producer</strong> <strong>MUST</strong> emit only <spanx style="verb">schema:"rapp/1-egg"</spanx> with a variant from §9.2, a §6.1 rappid, and, for
ZIP variants, a <spanx style="verb">contents</spanx> list whose every hash verifies. It <strong>MUST NOT</strong> emit any legacy egg schema.</t>
  <t><strong>Consumer</strong> <strong>MUST</strong> read every §9.2 variant, dispatch on <spanx style="verb">variant</spanx>, verify <strong>integrity then viability</strong> —
(0) the manifest is a §4 value satisfying <strong>every §9.1 rule</strong> — exact member set, <spanx style="verb">path</spanx> grammar (no <spanx style="verb">..</spanx>,
no leading <spanx style="verb">/</spanx>, no backslash), no duplicate paths, sort order, and for ZIP variants the archive entry set
equals <spanx style="verb">contents</spanx> ∪ {<spanx style="verb">manifest.json</spanx>} in the §9.1 deterministic order (this is the zip-slip defense; an
unenforced path grammar defends nothing); (1) every <spanx style="verb">contents[].hash</spanx> recomputes per §5; (2) the variant's
§9.2 structural requirements hold — and refuse whole on any failure; it <strong>MUST NOT</strong> reparent on transport.</t>
</list></t>

</section>
<section anchor="instantiation-instance-identity-and-grownfrom-lineage-rev-6-closes-the-hatch-seam"><name>Instantiation — instance identity and <spanx style="verb">grown_from</spanx> lineage (rev-6; closes the hatch seam)</name>
<t>An egg names an <strong>artifact</strong>; a hatch creates an <strong>instance</strong>. Rev-5 specified packing and reading but
left instantiation identity unwritten, so every implementation had to invent its own — this section is
that missing word.</t>

<t><list style="symbols">
  <t>The <spanx style="verb">rappid</spanx> in a packed egg's <spanx style="verb">rappid.json</spanx> (§9.2) names the <strong>artifact</strong>. Every hatch of the same
egg reads the <strong>same</strong> artifact identity (§6.2 canonicalize-on-read); re-minting it on hatch is
prohibited (§6.2).</t>
  <t>A consumer that instantiates an egg into a live installation <strong>MUST</strong> mint a <strong>fresh §6.2 identity
for the instance</strong>, exactly once, at first boot — from entropy, never derived from the artifact
identity, host name, or path. Two instances of one egg share the artifact identity and <strong>MUST NOT</strong>
share an instance identity.</t>
  <t>The instance's identity document <strong>SHOULD</strong> record <strong><spanx style="verb">grown_from</spanx></strong>: the §9.1 egg address
(64 lowercase hex) of the egg it was instantiated from. <spanx style="verb">grown_from</spanx> is written at mint time and is
<strong>immutable</strong> thereafter; it is <spanx style="verb">null</spanx> when the source is unknown; a producer <strong>MUST NOT</strong> fabricate
it — lineage is a fact about a birth, never a claim to invent. An instance that cannot say where it
came from says <spanx style="verb">null</spanx>.</t>
  <t><spanx style="verb">grown_from</spanx> is <strong>lineage, not inheritance</strong>: it confers no authority, no trust, and no §10 key
material. A reader <strong>MUST</strong> treat it as an unverified assertion unless the named egg is available and
its address recomputes per §9.1/§5.</t>
  <t>A newly authored artifact produced from one or more existing RAPP objects <strong>MAY</strong> record
<spanx style="verb">crossed_from</spanx> in its packaged <spanx style="verb">rappid.json</spanx>. The value is a sorted, duplicate-free array of typed
addresses <spanx style="verb">{space, hash}</spanx>, where <spanx style="verb">space</spanx> is <spanx style="verb">"rapp/1:particle"</spanx> or <spanx style="verb">"rapp/1:egg-manifest"</spanx> and <spanx style="verb">hash</spanx>
is 64 lowercase hex. One parent denotes an <strong>offspring</strong>; two or more denote a <strong>cross</strong>. The new
artifact <strong>MUST</strong> mint its own §6.2 rappid; parent artifacts and identities remain unchanged.
<spanx style="verb">crossed_from</spanx> is lineage only: it transfers no signature authority, ownership, entitlement, secret,
or trust. A verifier treats each parent as unverified until the addressed object is available and
recomputes in its declared space.</t>
</list></t>

</section>
</section>
<section anchor="trust-and-signatures-l2"><name>Trust and signatures (L2)</name>
<t><spanx style="verb">sig</spanx> is OPTIONAL on memory/body streams and REQUIRED on swarm streams (§8). Chain integrity comes from the
hash links (§5), not signatures. When present, <spanx style="verb">sig</spanx> <strong>MUST</strong> be JWS Compact Serialization with <strong>detached,
unencoded payload</strong> (<xref target="RFC7515"/> App. F + <xref target="RFC7797"/>):
- protected header members are <strong>exactly</strong> <spanx style="verb">alg</spanx> (<spanx style="verb">"EdDSA"</spanx> or <spanx style="verb">"ES256"</spanx>), <spanx style="verb">b64</spanx> (<spanx style="verb">false</spanx>), <spanx style="verb">crit</spanx>
  (<spanx style="verb">["b64"]</spanx>), <spanx style="verb">kid</spanx> (signer's §6.1 rappid), no others; the header octets <strong>MUST</strong> be <spanx style="verb">canonical(header)</spanx>
  (§4 — JCS orders them <spanx style="verb">alg</spanx>, <spanx style="verb">b64</spanx>, <spanx style="verb">crit</spanx>, <spanx style="verb">kid</spanx>, no whitespace);
- the <spanx style="verb">sig</spanx> string is the detached compact form <spanx style="verb">BASE64URL(canonical(header)) || ".." || BASE64URL(signature)</spanx>;
- JWS signing input = <spanx style="verb">BASE64URL(canonical(header)) || "." || canonical(frame \ {sig})</spanx>;
- <spanx style="verb">alg</spanx>: <spanx style="verb">EdDSA</spanx>/Ed25519 <xref target="RFC8037"/> or <spanx style="verb">ES256</spanx> <xref target="RFC7518"/>; ES256 signers <strong>SHOULD</strong> sign deterministically
  <xref target="RFC6979"/> (Ed25519 is deterministic by construction) so signed frame files stay byte-reproducible.</t>

<t><strong>Key discovery.</strong> A keyed rappid's tail is one-way (<spanx style="verb">Hb("rapp/1:rappid",SPKI)</spanx>). A verifier resolves the
signer's SPKI (DER) <strong>from the §13 registry</strong> entry (<spanx style="verb">rappid</spanx> → <spanx style="verb">spki_der_b64</spanx>); the door-of-record
<spanx style="verb">rappid.json</spanx> is the publication venue the registry entry is generated from, not itself a verification
source. The verifier <strong>MUST</strong> check <spanx style="verb">Hb("rapp/1:rappid", SPKI_DER)</spanx> == the rappid's tail and refuse on
mismatch or registry absence.</t>

<t><strong>Key lifecycle.</strong> Rotation is an identity re-anchor (§6.3, case <spanx style="verb">rotation</spanx>) with new <spanx style="verb">tail =
Hb("rapp/1:rappid", newSPKI)</spanx> and a §13.3 re-anchor record. A re-anchor <strong>deprecates</strong> the superseded
rappid's §13 <spanx style="verb">spki</spanx> entry: a verifier <strong>MUST</strong> refuse a <spanx style="verb">sig</spanx> whose <spanx style="verb">kid</spanx> is a superseded rappid on any
frame with <spanx style="verb">utc</spanx> ≥ the re-anchor record's <spanx style="verb">utc</spanx> (rotation gives forward security; earlier frames verify as
before). Compromise is declared by an owner-signed <strong>tombstone</strong> in the §13 registry <spanx style="verb">{rappid, revoked_utc}</spanx>; a verifier <strong>MUST</strong> refuse any <spanx style="verb">sig</spanx> by a
tombstoned key on a frame whose <spanx style="verb">utc</spanx> ≥ <spanx style="verb">revoked_utc</spanx>, checking tombstones at verification time (§7.5 step
6). "Owner-signed" means a <spanx style="verb">sig</spanx> verifying with <spanx style="verb">kid</spanx> == the registry's designated <spanx style="verb">estate_owner</spanx> rappid
(§13). A consumer <strong>MUST NOT</strong> infer authorship from an unsigned frame (keyless rappids assert location,
not authorship).</t>

</section>
<section anchor="conformance-classes"><name>Conformance classes</name>
<t><list style="symbols">
  <t><strong>Producer:</strong> emits only §4 JCS/I-JSON bytes, §5 domain-separated full-SHA-256 addresses, §6 rappids
minted per §6.2, §7.1 eleven-key frames, §9 <spanx style="verb">rapp/1-egg</spanx> variants — and <strong>no legacy form</strong>.</t>
  <t><strong>Consumer:</strong> runs the full §7.5 checklist (incl. 1a binding), §9.3 egg verification, §10 signature +
key-discovery + tombstone checks, canonicalizes legacy ids on read (§6.3), refuses on any failure, never
repairs/reparents/rolls back (§7.6).</t>
  <t><strong>Router/Mirror:</strong> invents no endpoints (§8), declares subordination to
<spanx style="verb">kody-w/rapp-1</spanx> for protocol semantics, and serves only
provenance-stamped hash-matching mirrors. Estate-specific product scope is
resolved separately through that estate's signed registry and master plan.</t>
</list></t>

<section anchor="immutable-grail-kernel-conformance"><name>Immutable Grail kernel conformance</name>
<t>An estate declares a Grail kernel through the exact §13.3 <spanx style="verb">grail-kernel</spanx> entry. Its <spanx style="verb">release_scope</spanx>
selects the release family to which the pin applies; a registry <strong>MUST</strong> contain at most one
<spanx style="verb">grail-kernel</spanx> entry for any release scope. Its entry-level <spanx style="verb">sig</spanx>
<strong>MUST</strong> verify under <spanx style="verb">declared_by</spanx>, which <strong>MUST</strong> be the estate owner in effect at <spanx style="verb">activated_utc</spanx>
(§13.2); <spanx style="verb">activated_utc</spanx> <strong>MUST NOT</strong> be more than 300 seconds after the verifier's first-seen time for
that entry. The pin activates locally when a consumer first accepts an authenticated, non-rollback
registry containing that valid declaration. Until then it is structural evidence only. The Grail is
identified by <spanx style="verb">grail_id</spanx>, never by a mutable branch, tag, repository name, or product label. Once
activated, the pin is a permanent compatibility anchor, not a moving release channel:</t>

<t><list style="numbers" type="1">
  <t>Every conformance evaluation <strong>MUST</strong> receive <spanx style="verb">release_scope</spanx> from an authenticated owner-controlled
release policy, never from candidate bytes or candidate-controlled configuration, then resolve its
one <spanx style="verb">grail_id</spanx> exclusively from the activated, persisted §13.3 binding. If the policy repeats
<spanx style="verb">grail_id</spanx>, that value is only a consistency assertion and <strong>MUST</strong> byte-equal the registry result; it
cannot select or rebind the pin. Every ring and release/deployment stage, including development,
qualification, Preprod, installation, and production verification, <strong>MUST</strong> compare the candidate's
Grail path to that exact binding, SHA-256, and byte length.</t>
  <t>A missing, changed, substituted, or unmeasured Grail byte is <spanx style="verb">kernel-drift</spanx> and <strong>MUST</strong> fail closed.
Approval, urgency, compatibility claims, and semantic equivalence cannot waive byte inequality.</t>
  <t>A pipeline <strong>MUST NOT</strong> silently restore the pinned file after testing a different file. The exact
release-shaped artifact containing the immutable Grail bytes <strong>MUST</strong> itself pass the release gates.</t>
  <t>Every gate <strong>MUST</strong> resolve every archive, container, wrapper, environment override, symlink, and
filesystem indirection to one regular runtime entry-point file under the immutable release root;
refuse links and ambiguous alternatives; verify its bytes; and bind the release digest, resolved
entry-point path, <spanx style="verb">release_scope</spanx>, <spanx style="verb">grail_id</spanx>, raw SHA-256, and byte length into its evidence.</t>
  <t>Every gate that executes the kernel <strong>MUST</strong> bind verification atomically to execution from a sealed
immutable object or snapshot. An already-open descriptor is sufficient only when its backing bytes
are under platform-enforced write exclusion for the entire verification-through-consumption interval
and the interpreter/loader consumes that exact descriptor without reopening a pathname. An immutable
content-addressed release root is sufficient only when no concurrent principal can modify it. A
hash-then-path-launch without one of those guarantees is nonconformant. At least one execution gate for
every supported production platform <strong>MUST</strong> perform this proof before release approval. Merely
carrying an unused matching file is insufficient. A stored transpiled, bundled, generated, or patched
derivative is different kernel bytes and requires a new <spanx style="verb">grail_id</spanx>; ordinary interpreter/JIT
compilation directly from the pinned bytes is permitted when no alternate stored kernel is selected.</t>
  <t>New implementation behavior <strong>MUST</strong> live outside the Grail kernel and <strong>MUST NOT</strong> alter any
RAPP-visible canonical form or wire semantic except through the registration and evolution rules of
§§7, 9, 12, and 13.</t>
  <t>If required behavior cannot be expressed outside the pin, the implementation <strong>MUST</strong> report an
incompatibility. It <strong>MUST NOT</strong> edit the Grail and continue under the same identity.</t>
  <t>Different kernel bytes produce a different <spanx style="verb">grail_id</spanx> and therefore are a different Grail. A new
<spanx style="verb">grail-kernel</spanx> entry may coexist and may name the old <spanx style="verb">grail_id</spanx> as <spanx style="verb">predecessor</spanx>, but it <strong>MUST NOT</strong>
overwrite, retag, alias, or redefine the prior Grail. A successor uses a new <spanx style="verb">release_scope</spanx>; an
existing scope is never rebound. A non-null <spanx style="verb">predecessor</spanx> <strong>MUST</strong> name an earlier accepted
<spanx style="verb">grail-kernel</spanx> entry and <strong>MUST NOT</strong> form a cycle. A registry <strong>MUST</strong> contain at most one
<spanx style="verb">grail-kernel</spanx> entry for each <spanx style="verb">grail_id</spanx>.</t>
  <t>On first activation a consumer <strong>MUST</strong> persist the full canonical <spanx style="verb">grail-kernel</spanx> entry. Every later
accepted registry <strong>MUST</strong> retain that entry byte-for-byte. Removal, mutation, or a second entry for
the same <spanx style="verb">grail_id</spanx> is a permanent <spanx style="verb">kernel-drift</spanx> refusal even when <spanx style="verb">registry_seq</spanx> increased.</t>
</list></t>

<t>This rule governs release topology rather than the §8 wire shape, so it does not add a frame member,
endpoint, compatibility shim, or alternate <spanx style="verb">rapp/1</spanx> encoding.</t>

</section>
<section anchor="operational-conformance-profiles"><name>Operational conformance profiles</name>
<t>RAPP/1 defines the substrate. Two subordinate operational profiles define how a production AI changes
without mutating that substrate or the serving system underneath users:</t>

<t><list style="symbols">
  <t><strong>RAPP CI/CD</strong> (<spanx style="verb">rapp-cicd/1</spanx>) — <spanx style="verb">protocols/rapp-cicd/1/SPEC.md</spanx>; immutable release capsules,
ordered qualification evidence, exact-candidate promotion, Preprod, and rollback/restore proof.</t>
  <t><strong>RAPP Deploy</strong> (<spanx style="verb">rapp-deploy/1</spanx>) — <spanx style="verb">protocols/rapp-deploy/1/SPEC.md</spanx>; isolated serving/candidate
lineages, cellular rollout, progressive exposure, expiring AI-health evidence, quarantine, and exact
rollback.</t>
</list></t>

<t>The profiles are RAPP/1 applications, not alternate wire versions:</t>

<t><list style="numbers" type="1">
  <t>Their payloads <strong>MUST</strong> be §4 canonical I-JSON and are identified by
<spanx style="verb">H("rapp/1:particle", payload)</spanx>.</t>
  <t>Authoritative profile payloads <strong>MUST</strong> travel in signed RAPP/1 frames using a registered compatible
kind. Local unsigned files are drafts, fixtures, or caches and cannot authorize promotion or traffic.</t>
  <t>They <strong>MUST NOT</strong> add a transport endpoint beside §8 <spanx style="verb">POST /chat</spanx>, redefine any RAPP/1 primitive, or
weaken §11.1.</t>
  <t>An estate activates a profile through an authenticated §13.3 <spanx style="verb">protocol</spanx> entry pinning its exact
repository, path, and SHA-256. A moving branch is discovery, never authority.</t>
  <t>A claim of <strong>RAPP production conformance</strong> requires both profiles. Core Producer, Consumer, and
Router/Mirror implementations remain conformant without implementing production operations.</t>
  <t>Fixed envelope keys carry the safety invariants. Policy-defined check identifiers, component kinds,
health objectives, and resilience controls are extension points; adding one does not change the
profile token or the RAPP/1 wire.</t>
  <t>A consumer <strong>MUST</strong> refuse unknown required policy semantics. It may preserve and relay unknown
optional evidence, but may not treat it as satisfying a requirement it does not implement.</t>
</list></t>

<t>The executable reference validators are <spanx style="verb">rapp_cicd.py</spanx> and <spanx style="verb">rapp_deploy.py</spanx>; controlled profile vectors
are in <spanx style="verb">operations_conformance.py</spanx>. JSON Schemas define structural shape, while the reference validators
enforce ordering, identity, temporal, and cross-document rules that JSON Schema cannot express.</t>

</section>
</section>
<section anchor="versioning-evolution-no-legacy"><name>Versioning, evolution, no-legacy</name>
<t>RAPP is a <strong>living standard</strong> (WHATWG): revised in place, never forked into parallel versions; a <spanx style="verb">name/X.Y</spanx>
label <strong>MUST NOT</strong> ever denote two shapes (Art. II) — a shape change moves the token (§7.1). Published
content-addressed artifacts are <strong>immutable</strong> (SemVer/crates). Because the estate has <strong>no uncontrolled
userspace</strong>, there is <strong>no perpetual backward compatibility</strong> (Art. III): a change to a canonical form is a
<strong>total migration</strong> of every instance + <strong>deletion</strong> of the old form. Sealed re-genesis history (§12.1) is
the retained live-stream exception and is not "legacy compatibility."</t>

<t>Immutable specification-governance history is a separate narrow exception:
rev-5 through rev-13 anchor frames retain interpretable immutable pointer
payloads so their ratified normative bytes remain resolvable. They are
historical authority records, not accepted live legacy protocol forms. A
producer <strong>MUST NOT</strong> emit the pointer-only revision profile after rev-13.</t>

<section anchor="re-genesis-converging-an-immutable-chain-one-owner-authorized-operation"><name>Re-genesis (converging an immutable chain — one owner-authorized operation)</name>
<t><list style="numbers" type="1">
  <t><strong>Terminal seal:</strong> <spanx style="verb">seal = Hb("rapp/1:seal", head_octets)</spanx>. <spanx style="verb">head_octets</spanx> is the exact octets of the old
head's record <strong>as retained under <spanx style="verb">legacy/</spanx></strong> (step 4): for a one-frame-per-file store, the retired
file's full octets; for a line-oriented log, the head's line <strong>excluding</strong> its trailing terminator. The
retained <spanx style="verb">legacy/</spanx> artifact is the verification reference for the seal, and retirement <strong>MUST</strong> preserve
those octets bit-exact. The step-3 <spanx style="verb">genesis</spanx> registry entry <strong>SHOULD</strong> record the <spanx style="verb">legacy/</spanx> artifact's
repo+path so a consumer <strong>MAY</strong> verify the seal against it. (Defined for every legacy shape, including
ones that cannot be §7.3-hashed.)</t>
  <t><strong>New genesis:</strong> emit <spanx style="verb">seq</spanx>=0, <spanx style="verb">prev</spanx>=null, <spanx style="verb">kind</spanx> = the registered re-genesis kind <strong>of the stream's
family</strong> — <spanx style="verb">memory.re-genesis</spanx>, <spanx style="verb">swarm.re-genesis</spanx>, or <spanx style="verb">body.re-genesis</spanx> (three §13 kinds, used only
here) so the frame satisfies §7.2 family↔stream compatibility for any stream — <spanx style="verb">sig</spanx>≠null owner-signed
(§10, §13 <spanx style="verb">estate_owner</spanx>), <spanx style="verb">payload</spanx> = <spanx style="verb">{"migrated_from":{"stream_id":&lt;old&gt;,"terminal_seal":&lt;seal&gt;,
"terminal_seq":&lt;n&gt;}}</spanx> and no other members. A consumer <strong>MUST</strong> treat any <spanx style="verb">*.re-genesis</spanx> kind as the sole
re-genesis marker for its family and refuse an unsigned/non-owner one.</t>
  <t><strong>Register (the linearization point):</strong> append a §13.3 <spanx style="verb">genesis</spanx> entry mapping the <spanx style="verb">stream_id</spanx> to the new
genesis's <spanx style="verb">frame_hash</spanx>, <strong>and flag every prior <spanx style="verb">genesis</spanx> entry for that <spanx style="verb">stream_id</spanx> <spanx style="verb">deprecated</spanx></strong> — the
first convergence included (it deprecates the creation-time genesis), so exactly one non-deprecated entry
always remains. A consumer resolves the current genesis <strong>only</strong> via that sole non-deprecated entry. A
<strong>concurrent</strong> second registration fails closed (the append is the linearization point, Art. IX); a
later fork/brick (§7.6/§14) is a fresh owner-authorized convergence that appends again and re-deprecates.</t>
  <t><strong>Retire:</strong> move old frames under <spanx style="verb">legacy/</spanx> — retained as immutable sealed history, never served as
current, never extended, never read as live chain. No live frame may set <spanx style="verb">prev</spanx>/<spanx style="verb">prev_wave</spanx> to a retired
hash (a dangling ref is a drift finding).</t>
  <t>Keep the old <spanx style="verb">stream_id</spanx> unless the identity itself re-anchored (§6.3), in which case the registry entry
also records <spanx style="verb">old_stream_id → new_stream_id</spanx>.</t>
  <t>Two frames with equal <spanx style="verb">stream_id</spanx>+<spanx style="verb">seq</spanx> from different eras are disambiguated <strong>solely</strong> by descent from
the current registered genesis. Re-genesis is one-time per convergence; a repeat <em>of the same
convergence</em> is the concurrent case (step 3, fails closed).</t>
</list></t>

</section>
<section anchor="dogg-specification-revision-chain"><name>DOGG specification revision chain</name>

<t>The RAPP/1 revision content is the append-only JSONL stream at
<spanx style="verb">anchor/chain.jsonl</spanx>. The chain's particle, wave, and predecessor hashes prove
byte integrity and linear history; they do <strong>not</strong> authenticate who selected a
head or ratified an amendment. Until a separately authenticated RAPP
registry/checkpoint is ratified, authority is selected by owner-ratified
acceptance of a chain snapshot onto protected canonical
<spanx style="verb">refs/heads/main</spanx> at <spanx style="verb">https://github.com/kody-w/rapp-1</spanx>. A consumer starting
from an out-of-band pinned accepted commit and head frame hash has the same
immutable selection evidence. An internally valid fork is not authoritative
merely because its hashes verify.</t>

<t>Every protocol adjustment <strong>MUST</strong> append exactly one valid successor frame:</t>

<t><list style="symbols">
  <t><spanx style="verb">spec</spanx> remains <spanx style="verb">"rapp/1"</spanx>, the envelope remains exactly eleven keys, and the
allowed registered anchor kind is <spanx style="verb">body.pulse</spanx>; this profile does not create
a new kind or alter canonicalization, hashing, or registered-kind semantics;</t>
  <t><spanx style="verb">stream_id</spanx> remains the bootstrap-pinned anchor stream, <spanx style="verb">seq</spanx> is contiguous,
<spanx style="verb">prev</spanx> names the predecessor's <spanx style="verb">payload_hash</spanx>, <spanx style="verb">prev_wave</spanx> and <spanx style="verb">sig</spanx> are
<spanx style="verb">null</spanx>, and the applicable §7.5 integrity checks hold;</t>
  <t>the frame's <spanx style="verb">frame_hash</spanx> is the durable protocol-revision identity.
Human names such as <spanx style="verb">rev-14</spanx> are lookup labels/views, never identities.</t>
</list></t>

<section anchor="immutable-bootstrap-boundary"><name>Immutable bootstrap boundary</name>

<t>Verification begins from the frozen <spanx style="verb">rapp-anchor-bootstrap/1</spanx> profile published
through <spanx style="verb">anchor/bootstrap/index.json</spanx>. The index names a
<spanx style="verb">anchor/bootstrap/sha256-&lt;raw-profile-sha256&gt;.json</spanx> object and pins the exact
stdlib verifier at <spanx style="verb">anchor/bootstrap_verify.py</spanx> by raw SHA-256 and byte length.
The profile fixes the exact-integer JCS subset and limits, particle/wave
domains, eleven frame keys, timestamp/sequence/predecessor rules, canonical
repository and protected ref, anchor stream ID, genesis frame and payload
hashes, <spanx style="verb">body.pulse</spanx> anchor profile, null signature/wave fields, and byte/depth
limits needed to verify this chain.</t>

<t>The content-addressed bootstrap profile is immutable. Changing it requires a
new bootstrap schema/version and a new external ratification; mutating
<spanx style="verb">rapp-anchor-bootstrap/1</spanx> in place is refusal. Its initial authenticity is
necessarily external — owner-ratified protected canonical main or an
out-of-band pinned profile hash/immutable commit — because neither a chain nor
code fetched with that chain can circularly authenticate its own parser.
<spanx style="verb">rapp.py</spanx> remains the complete reference implementation and is cross-checked,
but it is not the mutable bootstrap trust pin.</t>

</section>
<section anchor="revision-payloads-and-historical-exception"><name>Revision payloads and historical exception</name>

<t>Frames through rev-13 retain their immutable legacy pointer fields:
<spanx style="verb">canonical_repo</spanx>, a full 40-lowercase-hex <spanx style="verb">commit</spanx>, safe relative
<spanx style="verb">normative_path</spanx>, raw-file <spanx style="verb">normative_sha256</spanx>, and decimal-string
<spanx style="verb">normative_bytes</spanx>. A resolver <strong>MUST</strong> construct only an immutable
commit-pinned GitHub Raw URL from those fields, fetch the bytes, and verify
length and raw SHA-256 before use. A branch or tag name in <spanx style="verb">commit</spanx>, an unsafe
path, malformed UTF-8, a byte-order mark, or a hash/length mismatch is refusal.
Path validation occurs on the original string before path-library
normalization: empty, absolute, <spanx style="verb">.</spanx>/<spanx style="verb">..</spanx>, empty components, repeated slash,
leading <spanx style="verb">./</spanx>, trailing slash, backslash, percent-encoded ambiguity, or any
value whose reconstructed POSIX form differs is refused.
These pointer payloads are immutable governance history under Article 3's
narrow exception; they <strong>MUST NOT</strong> be emitted for rev-14 or later.</t>

<t>Rev-14 and each successor using this profile carries all normative text inside
the chain frame. Its payload <strong>MUST</strong> include
<spanx style="verb">schema:"rapp-spec-revision/1"</spanx>, a unique <spanx style="verb">revision</spanx>, and both
<spanx style="verb">previous_revision</spanx> and <spanx style="verb">previous_normative_sha256</spanx> matching the immediate
predecessor. It preserves the useful legacy fields above, carries the
publication/ratification metadata defined below, and adds exactly this
<spanx style="verb">normative</spanx> object:</t>

<t><spanx style="verb">json
{
  "media_type": "text/markdown; charset=utf-8",
  "text": "&lt;exact normative SPEC.md Unicode text&gt;",
  "sha256": "&lt;lowercase SHA-256 of text encoded as UTF-8&gt;",
  "bytes": 123
}
</spanx></t>

<t><spanx style="verb">normative.text</spanx> <strong>MUST</strong> encode as UTF-8 without a byte-order mark;
<spanx style="verb">normative.sha256</spanx> and <spanx style="verb">normative.bytes</spanx> <strong>MUST</strong> match those exact octets and
the corresponding legacy <spanx style="verb">normative_sha256</spanx> and <spanx style="verb">normative_bytes</spanx>. The complete
canonical frame remains subject to §4's 1 MiB limit. Inline revisions require
only the selected, verified chain bytes once fetched; their commit/path fields
are immutable provenance, not a second source for normative text.</t>

<t>The payload's <spanx style="verb">publication</spanx> object, repeated in the beacon and revision index,
records: canonical repository <spanx style="verb">https://github.com/kody-w/rapp-1</spanx>; protected ref
<spanx style="verb">refs/heads/main</spanx>; selection by owner-ratified acceptance; the accepted
canonical-main commit as linearization point; prohibition of history
replacement; mandatory rebase/regeneration for a competing append; rev-14
ratification under rev-13 Article 14; application of this chain-append process
from rev-15 onward; and <spanx style="verb">authenticated_registry_checkpoint:null</spanx> until one
actually exists. This metadata <strong>MUST NOT</strong> be interpreted as a signature or
authenticated registry.</t>

</section>
<section anchor="content-addressed-publication-and-resolution"><name>Content-addressed publication and resolution</name>

<t>Every chain frame <strong>MUST</strong> also be published, without a trailing line
terminator, at:</t>

<t><spanx style="verb">text
anchor/frames/&lt;frame_hash&gt;.json
</spanx></t>

<t>A fetch through mutable main is discovery only. Given frame hash <spanx style="verb">F</spanx>, a
stranger may fetch
<spanx style="verb">https://raw.githubusercontent.com/kody-w/rapp-1/main/anchor/frames/F.json</spanx>,
parse it under the pinned bootstrap, and accept the object only if its computed
wave equals <spanx style="verb">F</spanx>. For accepted snapshot commit <spanx style="verb">C</spanx>, the immutable URL is
<spanx style="verb">https://raw.githubusercontent.com/kody-w/rapp-1/C/anchor/frames/F.json</spanx>.
Wrong content at the correct hash-derived path is refusal.</t>

<t><spanx style="verb">anchor/index.json</spanx> is a deterministic generated index from <spanx style="verb">seq</spanx>, revision
label, frame hash, and payload hash to those objects. It carries no independent
authority: a consumer <strong>MUST</strong> verify each selected object and match the index
and beacon to the fully verified chain. Resolution by frame hash uses the path
algorithm directly; resolution by payload hash or sequence uses the verified
index; resolution by revision label is a view over it. The fixed pre-profile
rev-5 pulses share one historical label, so the <spanx style="verb">rev-5</spanx> view resolves to the
greatest matching <spanx style="verb">seq</spanx>, while every pulse remains directly resolvable by
sequence and hashes.</t>

<t>A consumer <strong>MUST</strong> verify the chain from the bootstrap-pinned genesis before
using a revision as selected history. It <strong>MUST</strong> refuse an invalid frame shape,
particle, wave, or <spanx style="verb">prev</spanx>; a fork or duplicate <spanx style="verb">seq</spanx>; duplicate frame/payload
hashes; a duplicate profiled revision; an unsupported schema; a legacy
pointer-only frame after rev-13; malformed UTF-8; normative hash/length drift;
bootstrap/index/object drift; or an object absent from the selected chain.</t>

<t><spanx style="verb">orient.json</spanx> is only a beacon to the head. Its sequence, frame hash, payload
hash, bootstrap/index pins, authority-selection metadata, and every retained
head mirror — including <spanx style="verb">registered_kinds</spanx>, vocabulary, operational profiles,
foundation, philosophy, and Constitution metadata — <strong>MUST</strong> be regenerated
from and match the verified head. While the beacon schema remains
<spanx style="verb">rapp/1-anchor</spanx>, <spanx style="verb">spec.normative_path</spanx> <strong>MUST</strong> remain as a compatibility alias
equal to <spanx style="verb">spec.materialized_path</spanx>. <spanx style="verb">SPEC.md</spanx> <strong>MUST</strong> reproduce the selected
head byte-for-byte. The Atom feed and mutable main URLs are discovery only.</t>

<t>Materialization and immutable-pointer caching <strong>MUST</strong> use bounded reads and
same-directory atomic replacement without following a symlink in the
destination leaf or path. A symlink, unsafe directory component, oversized
cache entry, content change during read/write, or compare-and-swap mismatch is
refusal; the requested leaf is replaced, never the symlink target.</t>

<t>After acceptance, let <spanx style="verb">C</spanx> be the full 40-hex canonical-main commit containing
the accepted snapshot. The immutable checkpoint URLs are:</t>

<t><spanx style="verb">text
https://raw.githubusercontent.com/kody-w/rapp-1/C/anchor/chain.jsonl
https://raw.githubusercontent.com/kody-w/rapp-1/C/anchor/orient.json
https://raw.githubusercontent.com/kody-w/rapp-1/C/anchor/index.json
</spanx></t>

<t>The authority linearization point is the owner-ratified acceptance of <spanx style="verb">C</spanx> onto
protected canonical main, not creation of a local frame, a feed event, or a
hash alone. A stale competing append <strong>MUST</strong> rebase onto the accepted head and
regenerate. Force-push or history replacement of accepted authority is
prohibited.</t>

</section>
<section anchor="rev-14-transition-and-draft-replacement"><name>Rev-14 transition and draft replacement</name>

<t>Rev-14 is ratified under rev-13 Article 14. Owner acceptance of the prepared
change onto canonical protected main makes the final rev-14 frame effective;
the new chain-append process governs rev-15 onward. Before that acceptance,
rev-14 artifacts are unpublished drafts. A later commit in the same
owner-ratified change may deterministically replace the one unpublished rev-14
draft line/object and update dependent hashes, but <strong>MUST</strong> preserve every
rev-5 through rev-13 line byte-for-byte and <strong>MUST NOT</strong> represent the replaced
draft as accepted history.</t>

<t>The publication generator <strong>MUST</strong> hold one cross-process exclusive lock across
read, verification, generation, comparison, and publication. Immediately
before replacing <spanx style="verb">chain.jsonl</spanx> it <strong>MUST</strong> re-read and verify the on-disk head
and compare the complete expected prefix; a stale writer or changed prefix is
refused. Supporting content-addressed artifacts are published first, the
authority chain is compare-and-swap replaced next, and the beacon is replaced
last, with file and parent-directory synchronization. Because the chain is
authority content and the beacon is only a derived view, interruption after a
valid chain replacement may leave a stale or missing beacon; the next locked
run <strong>MUST</strong> deterministically regenerate it from the verified chain rather
than refusing the valid head forever.</t>

<t>This chain and beacon are unsigned. They provide integrity after authority
selection, not authorship, and implementations <strong>MUST NOT</strong> fabricate a
signature or authenticated registry/checkpoint state for them.</t>

</section>
</section>
</section>
<section anchor="the-registry-an-estates-signed-root-of-trust-append-only"><name>The registry — an estate's signed root of trust (append-only)</name>
<t>Each estate selects an owner-controlled <spanx style="verb">canonical_source</spanx> for its
<spanx style="verb">schema:"rapp/1-registry"</spanx> document. The kody-w reference estate may publish
its registry through <spanx style="verb">kody-w/RAPP</spanx> or another owner-authorized location, but
that estate instance cannot alter this protocol. Because §7.6 head resets, §10
key discovery, tombstone revocation, and ownership all resolve through the
selected registry, it is the <strong>root of trust for that estate</strong> and is itself
authenticated (an unsigned mutable file at the root of the trust graph would
forge that estate).</t>

<section anchor="trust-anchor-and-registry-authentication"><name>Trust anchor and registry authentication</name>
<t><list style="symbols">
  <t>The one bootstrap axiom is the <strong><spanx style="verb">estate_owner</spanx> rappid string</strong> itself: since a keyed tail is
<spanx style="verb">Hb("rapp/1:rappid", SPKI_DER)</spanx>, the rappid <strong>is</strong> a self-certifying key fingerprint, distributed
out-of-band exactly once (QR, invite, docs) the way a root-CA certificate is.</t>
  <t>The registry document <strong>MUST</strong> carry a top-level <spanx style="verb">registry_seq</spanx> (uint53) and a detached §10 JWS <spanx style="verb">sig</spanx> over
<spanx style="verb">canonical(registry \ {sig})</spanx> with <spanx style="verb">kid</spanx> = the <spanx style="verb">estate_owner</spanx> rappid. A consumer <strong>MUST</strong> verify this
signature against an SPKI whose <spanx style="verb">Hb("rapp/1:rappid", SPKI_DER)</spanx> equals the anchor rappid's tail (the SPKI
may travel alongside the registry — the tail check authenticates it) and <strong>MUST</strong> refuse an unsigned or
non-verifying registry.</t>
  <t><strong>No rollback:</strong> a consumer persists the highest <spanx style="verb">registry_seq</spanx> it verified and refuses any registry with
a lower one (mirrors §7.6). <strong>Freshness:</strong> a consumer <strong>MUST</strong> obtain the registry from <spanx style="verb">canonical_source</spanx>
or a provenance-stamped (Art. VIII) mirror of it, <strong>SHOULD</strong> refresh before any §7.5-step-6 or §7.6
head-reset decision, and <strong>MUST</strong> report a verification made against a registry older than its staleness
policy as <em>stale</em>, not <em>clean</em>.</t>
</list></t>

</section>
<section anchor="owner-succession-time-scoped-authority"><name>Owner succession (time-scoped authority)</name>
<t>"Owner-signed" means: the <spanx style="verb">sig</spanx> verifies per §10 <strong>and</strong> <spanx style="verb">kid</spanx> is the estate-owner <strong>in effect at the
artifact's <spanx style="verb">utc</spanx></strong> — the current <spanx style="verb">estate_owner</spanx> or any predecessor reachable through the registry's
re-anchor records (§13.3), with the artifact's <spanx style="verb">utc</spanx> inside that owner's tenure <spanx style="verb">[record.utc, successor.utc)</spanx>.
Verification uses the owner in effect at the artifact's time, <strong>never only the current one</strong> (so a routine
owner key rotation never invalidates historical re-genesis frames or tombstones). Estate-owner <strong>root-key
compromise</strong> is recovered only by re-distributing a new trust anchor out-of-band (§13.1) — it cannot be
expressed inside the registry it signs.</t>

</section>
<section anchor="entry-types-each-a-4-value-document-schemarapp1-registry"><name>Entry types (each a §4 value; document <spanx style="verb">schema:"rapp/1-registry"</spanx>)</name>
<t>The registry is an I-JSON document; every entry is append-only (never removed/renamed; retirement is a
<spanx style="verb">deprecated:true</spanx> flag). Entry types and their exact members:
- <strong>protocol</strong> <spanx style="verb">{type:"protocol", name, spec_repo, spec_path, spec_hash, deprecated}</spanx> — an estate
  adoption pin, never a power to redefine a protocol. An entry with <spanx style="verb">name:"rapp/1"</spanx> that is used for a
  current-conformance claim <strong>MUST</strong> set <spanx style="verb">spec_repo:"https://github.com/kody-w/rapp-1"</spanx>,
  <spanx style="verb">spec_path:"SPEC.md"</spanx>, and <spanx style="verb">spec_hash</spanx> to a normative SHA-256 published by a verified frame in this
  repository's anchor chain. A historical RAPP/1 pin may be retained only as <spanx style="verb">deprecated:true</spanx>; it does
  not override the current anchor. Other protocol entries are subordinate to their own canonical
  authorities and <strong>MUST NOT</strong> claim the <spanx style="verb">rapp/1</spanx> name or namespace.
- <strong>kind</strong> <spanx style="verb">{type:"kind", kind, family, deprecated}</spanx> (incl. the three <spanx style="verb">*.re-genesis</spanx> kinds)
- <strong>egg-variant</strong> <spanx style="verb">{type:"egg-variant", variant, deprecated}</spanx> · <strong>error-code</strong> <spanx style="verb">{type:"error-code", code}</spanx>
  (both closed namespaces; unregistered value = not conformant)
- <strong>genesis</strong> <spanx style="verb">{type:"genesis", stream_id, frame_hash, deprecated, old_stream_id?, new_stream_id?}</spanx> — <strong>every</strong>
  stream registers its creation genesis; re-genesis appends a new one and deprecates all prior (§12.1 step 3);
  §7.6's "registered genesis" is the sole non-deprecated <spanx style="verb">genesis</spanx> for a <spanx style="verb">stream_id</spanx>.
- <strong>spki</strong> <spanx style="verb">{type:"spki", rappid, spki_der_b64, deprecated}</spanx> — the §10 key-discovery source.
- <strong>tombstone</strong> <spanx style="verb">{type:"tombstone", rappid, revoked_utc, sig}</spanx>, <spanx style="verb">sig</spanx> owner-signed over <spanx style="verb">canonical(entry \ {sig})</spanx>.
- <strong>re-anchor</strong> <spanx style="verb">{type:"re-anchor", old_rappid, new_rappid, case:("upgrade"|"rotation"|"compromise"|"tag-migrate"),
  utc, sig, old_key_sig?}</spanx> — <spanx style="verb">sig</spanx> owner-signed; <spanx style="verb">old_key_sig</spanx> a §10 JWS by the <strong>old</strong> key over
  <spanx style="verb">canonical(entry \ {sig,old_key_sig})</spanx>, REQUIRED for <spanx style="verb">case:"rotation"</spanx>. This is the normative succession record (§13.2).
- <strong>grail-kernel</strong> <spanx style="verb">{type:"grail-kernel", release_scope, grail_id, repository, immutable_ref,
  object_format, commit, path, mode, blob, sha256, size_bytes, activated_utc, predecessor, declared_by,
  sig}</spanx> — exactly these members. <spanx style="verb">release_scope</spanx> is an absolute HTTPS URI selected by the estate owner;
  no two entries may share it. <spanx style="verb">grail_id</spanx> is
  <spanx style="verb">"grail:" || Hb("rapp/1:grail", kernel_bytes)</spanx>; <spanx style="verb">repository</spanx> is an absolute HTTPS URI;
  <spanx style="verb">immutable_ref</spanx> is a full <spanx style="verb">refs/tags/...</spanx> name that <strong>MUST</strong> resolve exactly to <spanx style="verb">commit</spanx>;
  <spanx style="verb">object_format</spanx> is <spanx style="verb">"sha1"</spanx> or <spanx style="verb">"sha256"</spanx> and fixes the required lowercase hexadecimal length of
  <spanx style="verb">commit</spanx> and <spanx style="verb">blob</spanx>; <spanx style="verb">path</spanx> at <spanx style="verb">commit</spanx> <strong>MUST</strong> resolve through the repository tree to exactly one
  regular blob with <spanx style="verb">mode</spanx> <spanx style="verb">"100644"</spanx> or <spanx style="verb">"100755"</spanx> and object id <spanx style="verb">blob</spanx>;
  <spanx style="verb">path</spanx> is a relative NFC POSIX path with no empty, <spanx style="verb">"."</spanx>, or <spanx style="verb">".."</spanx> component; <spanx style="verb">sha256</spanx> is the raw
  kernel bytes' 64-lowercase-hex SHA-256; <spanx style="verb">size_bytes</spanx> is their positive <spanx style="verb">uint53</spanx> length;
  <spanx style="verb">activated_utc</spanx> has the exact §7.4 form; <spanx style="verb">predecessor</spanx> is null or another <spanx style="verb">grail_id</spanx>; <spanx style="verb">declared_by</spanx> is
  a keyed rappid; and <spanx style="verb">sig</spanx> is a detached §10 JWS whose protected <spanx style="verb">kid</spanx> equals <spanx style="verb">declared_by</spanx>, over
  <spanx style="verb">canonical(entry \ {sig})</spanx>. The entry is additionally covered by the registry's §13.1 signature. A
  consumer verifies the entry signer as the estate owner in effect at <spanx style="verb">activated_utc</spanx>, verifies the
  referenced bytes, recomputes both hashes, persists the canonical entry on first activation, applies
  §11.1, and refuses a missing/mutated prior binding, duplicate <spanx style="verb">grail_id</spanx>, or locator whose bytes
  disagree.
- <strong>estate_owner</strong> <spanx style="verb">{type:"estate_owner", rappid}</spanx> (exactly one non-deprecated) · <strong>master-plan</strong>
  <spanx style="verb">{type:"master-plan", repo, path}</spanx> (Fed. Const. Art. VII).</t>

<t>§7.5 steps 1–5 are time-independent (append-only lookups); <strong>only</strong> step 6 (tombstones) and §13.2 owner
tenure are time-scoped, and both are monotone given the §13.1 no-rollback rule.</t>

</section>
</section>
<section anchor="security-considerations"><name>Security considerations</name>
<t><list style="symbols">
  <t><strong>Integrity:</strong> every object is domain-separated content-addressed (§5); a hostile mirror cannot alter
bytes without breaking the hash, so <em>history is safe given a trusted head</em>.</t>
  <t><strong>Head freshness is not self-certifying:</strong> the chain authenticates history, not which head is current; a
hostile mirror may serve a stale/forked head. Consumers counter with the §7.6 monotonic-head rule; swarm
heads <strong>SHOULD</strong> be owner-signed.</t>
  <t><strong>Cross-stream replay:</strong> without §7.5 step 1a, any genesis/segment of stream A replays as stream B (seq=0,
prev=null always pass). Step 1a's stream binding is mandatory.</t>
  <t><strong>Address-space confusion:</strong> §5 domain tags make a particle, wave, egg, or rappid tail with equal hex
non-interchangeable; stores key by <spanx style="verb">(space, hash)</spanx>.</t>
  <t><strong>Canonicalization attacks:</strong> the §4 I-JSON input-domain profile (no duplicate keys, no lone surrogates,
exact binary64, no normalization ambiguity) removes hash-splitting and NFC-twin vectors.</t>
  <t><strong>Identity forgery / key compromise:</strong> authorship requires a keyed rappid + valid §10 <spanx style="verb">sig</spanx>; rotation is
§6.3 re-anchor (verifiable authorization, §6.3/§13.3 — a self-asserted <spanx style="verb">_migrated_from</spanx> is refused);
compromise is a §13 tombstone enforced at verify time. Because a tombstone gates on the frame's
producer-controlled <spanx style="verb">utc</spanx>, a compromised key can still emit frames stamped just below <spanx style="verb">revoked_utc</spanx>; after
a compromise the owner <strong>SHOULD</strong> advance affected stream heads (or re-genesis) past <spanx style="verb">revoked_utc</spanx>.</t>
  <t><strong>Root of trust:</strong> the registry is the estate's signed root (§13.1); it is authenticated by an owner
signature anchored to the out-of-band <spanx style="verb">estate_owner</spanx> rappid fingerprint, <spanx style="verb">registry_seq</spanx>-monotonic against
rollback, and freshness-checked (a stale registry silently un-revokes keys and hides re-geneses).</t>
  <t><strong>Producer-controlled <spanx style="verb">utc</spanx> (DoS/merge bias):</strong> a future-dated head can brick a stream (successors refused
as earlier) and bias UTC-first merges. A consumer <strong>SHOULD</strong> refuse a frame whose <spanx style="verb">utc</spanx> exceeds receipt
time by &gt;300 s, and adversarial-scope merges <strong>SHOULD</strong> rank by <spanx style="verb">min(utc, first-seen)</spanx>; a bricked stream
converges by re-genesis (§12.1).</t>
  <t><strong>Sealed-artifact limits:</strong> a public mirror sees ciphertext and metadata. AES-GCM protects confidentiality
only while the DEK remains secret; a recipient that has lawfully decrypted plaintext can copy it.
Revocation stops future wrapped-key release, not prior possession. Shared passwords, embedded master keys,
moving-branch URLs, nonce reuse under one DEK, and claims that bytecode cannot be inspected after
decryption are non-conformant security postures (§9.2.1).</t>
  <t><strong>Kernel substitution:</strong> testing mutable ring bytes and restoring the Grail only after approval creates
an untested release. §11.1 requires the release-shaped artifact to contain the pinned bytes before its
tests, attestations, and approval; a digest mismatch is a blocking <spanx style="verb">kernel-drift</spanx> finding.</t>
  <t><strong>Kernel registry rollback/rebinding:</strong> persisting only <spanx style="verb">registry_seq</spanx> does not preserve an activated
Grail declaration across a malicious higher-sequence registry. Consumers also persist every activated
canonical <spanx style="verb">grail-kernel</spanx> entry and reject its later removal, mutation, or duplication (§11.1).</t>
</list></t>

</section>


  </middle>

  <back>


<references title='References' anchor="sec-combined-references">

    <references title='Normative References' anchor="sec-normative-references">



<reference anchor="RFC2119">
  <front>
    <title>Key words for use in RFCs to Indicate Requirement Levels</title>
    <author fullname="S. Bradner" initials="S." surname="Bradner"/>
    <date month="March" year="1997"/>
    <abstract>
      <t>In many standards track documents several words are used to signify the requirements in the specification. These words are often capitalized. This document defines these words as they should be interpreted in IETF documents. This document specifies an Internet Best Current Practices for the Internet Community, and requests discussion and suggestions for improvements.</t>
    </abstract>
  </front>
  <seriesInfo name="BCP" value="14"/>
  <seriesInfo name="RFC" value="2119"/>
  <seriesInfo name="DOI" value="10.17487/RFC2119"/>
</reference>
<reference anchor="RFC3339">
  <front>
    <title>Date and Time on the Internet: Timestamps</title>
    <author fullname="G. Klyne" initials="G." surname="Klyne"/>
    <author fullname="C. Newman" initials="C." surname="Newman"/>
    <date month="July" year="2002"/>
    <abstract>
      <t>This document defines a date and time format for use in Internet protocols that is a profile of the ISO 8601 standard for representation of dates and times using the Gregorian calendar.</t>
    </abstract>
  </front>
  <seriesInfo name="RFC" value="3339"/>
  <seriesInfo name="DOI" value="10.17487/RFC3339"/>
</reference>
<reference anchor="RFC5280">
  <front>
    <title>Internet X.509 Public Key Infrastructure Certificate and Certificate Revocation List (CRL) Profile</title>
    <author fullname="D. Cooper" initials="D." surname="Cooper"/>
    <author fullname="S. Santesson" initials="S." surname="Santesson"/>
    <author fullname="S. Farrell" initials="S." surname="Farrell"/>
    <author fullname="S. Boeyen" initials="S." surname="Boeyen"/>
    <author fullname="R. Housley" initials="R." surname="Housley"/>
    <author fullname="W. Polk" initials="W." surname="Polk"/>
    <date month="May" year="2008"/>
    <abstract>
      <t>This memo profiles the X.509 v3 certificate and X.509 v2 certificate revocation list (CRL) for use in the Internet. An overview of this approach and model is provided as an introduction. The X.509 v3 certificate format is described in detail, with additional information regarding the format and semantics of Internet name forms. Standard certificate extensions are described and two Internet-specific extensions are defined. A set of required certificate extensions is specified. The X.509 v2 CRL format is described in detail along with standard and Internet-specific extensions. An algorithm for X.509 certification path validation is described. An ASN.1 module and examples are provided in the appendices. [STANDARDS-TRACK]</t>
    </abstract>
  </front>
  <seriesInfo name="RFC" value="5280"/>
  <seriesInfo name="DOI" value="10.17487/RFC5280"/>
</reference>
<reference anchor="RFC5869">
  <front>
    <title>HMAC-based Extract-and-Expand Key Derivation Function (HKDF)</title>
    <author fullname="H. Krawczyk" initials="H." surname="Krawczyk"/>
    <author fullname="P. Eronen" initials="P." surname="Eronen"/>
    <date month="May" year="2010"/>
    <abstract>
      <t>This document specifies a simple Hashed Message Authentication Code (HMAC)-based key derivation function (HKDF), which can be used as a building block in various protocols and applications. The key derivation function (KDF) is intended to support a wide range of applications and requirements, and is conservative in its use of cryptographic hash functions. This document is not an Internet Standards Track specification; it is published for informational purposes.</t>
    </abstract>
  </front>
  <seriesInfo name="RFC" value="5869"/>
  <seriesInfo name="DOI" value="10.17487/RFC5869"/>
</reference>
<reference anchor="RFC6979">
  <front>
    <title>Deterministic Usage of the Digital Signature Algorithm (DSA) and Elliptic Curve Digital Signature Algorithm (ECDSA)</title>
    <author fullname="T. Pornin" initials="T." surname="Pornin"/>
    <date month="August" year="2013"/>
    <abstract>
      <t>This document defines a deterministic digital signature generation procedure. Such signatures are compatible with standard Digital Signature Algorithm (DSA) and Elliptic Curve Digital Signature Algorithm (ECDSA) digital signatures and can be processed with unmodified verifiers, which need not be aware of the procedure described therein. Deterministic signatures retain the cryptographic security features associated with digital signatures but can be more easily implemented in various environments, since they do not need access to a source of high-quality randomness.</t>
    </abstract>
  </front>
  <seriesInfo name="RFC" value="6979"/>
  <seriesInfo name="DOI" value="10.17487/RFC6979"/>
</reference>
<reference anchor="RFC7405">
  <front>
    <title>Case-Sensitive String Support in ABNF</title>
    <author fullname="P. Kyzivat" initials="P." surname="Kyzivat"/>
    <date month="December" year="2014"/>
    <abstract>
      <t>This document extends the base definition of ABNF (Augmented Backus-Naur Form) to include a way to specify US-ASCII string literals that are matched in a case-sensitive manner.</t>
    </abstract>
  </front>
  <seriesInfo name="RFC" value="7405"/>
  <seriesInfo name="DOI" value="10.17487/RFC7405"/>
</reference>
<reference anchor="RFC7493">
  <front>
    <title>The I-JSON Message Format</title>
    <author fullname="T. Bray" initials="T." role="editor" surname="Bray"/>
    <date month="March" year="2015"/>
    <abstract>
      <t>I-JSON (short for "Internet JSON") is a restricted profile of JSON designed to maximize interoperability and increase confidence that software can process it successfully with predictable results.</t>
    </abstract>
  </front>
  <seriesInfo name="RFC" value="7493"/>
  <seriesInfo name="DOI" value="10.17487/RFC7493"/>
</reference>
<reference anchor="RFC7515">
  <front>
    <title>JSON Web Signature (JWS)</title>
    <author fullname="M. Jones" initials="M." surname="Jones"/>
    <author fullname="J. Bradley" initials="J." surname="Bradley"/>
    <author fullname="N. Sakimura" initials="N." surname="Sakimura"/>
    <date month="May" year="2015"/>
    <abstract>
      <t>JSON Web Signature (JWS) represents content secured with digital signatures or Message Authentication Codes (MACs) using JSON-based data structures. Cryptographic algorithms and identifiers for use with this specification are described in the separate JSON Web Algorithms (JWA) specification and an IANA registry defined by that specification. Related encryption capabilities are described in the separate JSON Web Encryption (JWE) specification.</t>
    </abstract>
  </front>
  <seriesInfo name="RFC" value="7515"/>
  <seriesInfo name="DOI" value="10.17487/RFC7515"/>
</reference>
<reference anchor="RFC7516">
  <front>
    <title>JSON Web Encryption (JWE)</title>
    <author fullname="M. Jones" initials="M." surname="Jones"/>
    <author fullname="J. Hildebrand" initials="J." surname="Hildebrand"/>
    <date month="May" year="2015"/>
    <abstract>
      <t>JSON Web Encryption (JWE) represents encrypted content using JSON-based data structures. Cryptographic algorithms and identifiers for use with this specification are described in the separate JSON Web Algorithms (JWA) specification and IANA registries defined by that specification. Related digital signature and Message Authentication Code (MAC) capabilities are described in the separate JSON Web Signature (JWS) specification.</t>
    </abstract>
  </front>
  <seriesInfo name="RFC" value="7516"/>
  <seriesInfo name="DOI" value="10.17487/RFC7516"/>
</reference>
<reference anchor="RFC7518">
  <front>
    <title>JSON Web Algorithms (JWA)</title>
    <author fullname="M. Jones" initials="M." surname="Jones"/>
    <date month="May" year="2015"/>
    <abstract>
      <t>This specification registers cryptographic algorithms and identifiers to be used with the JSON Web Signature (JWS), JSON Web Encryption (JWE), and JSON Web Key (JWK) specifications. It defines several IANA registries for these identifiers.</t>
    </abstract>
  </front>
  <seriesInfo name="RFC" value="7518"/>
  <seriesInfo name="DOI" value="10.17487/RFC7518"/>
</reference>
<reference anchor="RFC7797">
  <front>
    <title>JSON Web Signature (JWS) Unencoded Payload Option</title>
    <author fullname="M. Jones" initials="M." surname="Jones"/>
    <date month="February" year="2016"/>
    <abstract>
      <t>JSON Web Signature (JWS) represents the payload of a JWS as a base64url-encoded value and uses this value in the JWS Signature computation. While this enables arbitrary payloads to be integrity protected, some have described use cases in which the base64url encoding is unnecessary and/or an impediment to adoption, especially when the payload is large and/or detached. This specification defines a means of accommodating these use cases by defining an option to change the JWS Signing Input computation to not base64url- encode the payload. This option is intended to broaden the set of use cases for which the use of JWS is a good fit.</t>
      <t>This specification updates RFC 7519 by stating that JSON Web Tokens (JWTs) MUST NOT use the unencoded payload option defined by this specification.</t>
    </abstract>
  </front>
  <seriesInfo name="RFC" value="7797"/>
  <seriesInfo name="DOI" value="10.17487/RFC7797"/>
</reference>
<reference anchor="RFC8037">
  <front>
    <title>CFRG Elliptic Curve Diffie-Hellman (ECDH) and Signatures in JSON Object Signing and Encryption (JOSE)</title>
    <author fullname="I. Liusvaara" initials="I." surname="Liusvaara"/>
    <date month="January" year="2017"/>
    <abstract>
      <t>This document defines how to use the Diffie-Hellman algorithms "X25519" and "X448" as well as the signature algorithms "Ed25519" and "Ed448" from the IRTF CFRG elliptic curves work in JSON Object Signing and Encryption (JOSE).</t>
    </abstract>
  </front>
  <seriesInfo name="RFC" value="8037"/>
  <seriesInfo name="DOI" value="10.17487/RFC8037"/>
</reference>
<reference anchor="RFC8174">
  <front>
    <title>Ambiguity of Uppercase vs Lowercase in RFC 2119 Key Words</title>
    <author fullname="B. Leiba" initials="B." surname="Leiba"/>
    <date month="May" year="2017"/>
    <abstract>
      <t>RFC 2119 specifies common key words that may be used in protocol specifications. This document aims to reduce the ambiguity by clarifying that only UPPERCASE usage of the key words have the defined special meanings.</t>
    </abstract>
  </front>
  <seriesInfo name="BCP" value="14"/>
  <seriesInfo name="RFC" value="8174"/>
  <seriesInfo name="DOI" value="10.17487/RFC8174"/>
</reference>
<reference anchor="RFC8785">
  <front>
    <title>JSON Canonicalization Scheme (JCS)</title>
    <author fullname="A. Rundgren" initials="A." surname="Rundgren"/>
    <author fullname="B. Jordan" initials="B." surname="Jordan"/>
    <author fullname="S. Erdtman" initials="S." surname="Erdtman"/>
    <date month="June" year="2020"/>
    <abstract>
      <t>Cryptographic operations like hashing and signing need the data to be expressed in an invariant format so that the operations are reliably repeatable. One way to address this is to create a canonical representation of the data. Canonicalization also permits data to be exchanged in its original form on the "wire" while cryptographic operations performed on the canonicalized counterpart of the data in the producer and consumer endpoints generate consistent results.</t>
      <t>This document describes the JSON Canonicalization Scheme (JCS). This specification defines how to create a canonical representation of JSON data by building on the strict serialization methods for JSON primitives defined by ECMAScript, constraining JSON data to the Internet JSON (I-JSON) subset, and by using deterministic property sorting.</t>
    </abstract>
  </front>
  <seriesInfo name="RFC" value="8785"/>
  <seriesInfo name="DOI" value="10.17487/RFC8785"/>
</reference>
<reference anchor="RFC9562">
  <front>
    <title>Universally Unique IDentifiers (UUIDs)</title>
    <author fullname="K. Davis" initials="K." surname="Davis"/>
    <author fullname="B. Peabody" initials="B." surname="Peabody"/>
    <author fullname="P. Leach" initials="P." surname="Leach"/>
    <date month="May" year="2024"/>
    <abstract>
      <t>This specification defines UUIDs (Universally Unique IDentifiers) --
also known as GUIDs (Globally Unique IDentifiers) -- and a Uniform
Resource Name namespace for UUIDs. A UUID is 128 bits long and is
intended to guarantee uniqueness across space and time. UUIDs were
originally used in the Apollo Network Computing System (NCS), later
in the Open Software Foundation's (OSF's) Distributed Computing
Environment (DCE), and then in Microsoft Windows platforms.</t>
      <t>This specification is derived from the OSF DCE specification with the
kind permission of the OSF (now known as "The Open Group"). Information from earlier versions of the OSF DCE specification have
been incorporated into this document. This document obsoletes RFC
4122.</t>
    </abstract>
  </front>
  <seriesInfo name="RFC" value="9562"/>
  <seriesInfo name="DOI" value="10.17487/RFC9562"/>
</reference>



    </references>

    <references title='Informative References' anchor="sec-informative-references">

<reference anchor="RAPP1-ANCHOR" target="https://kody-w.github.io/rapp-1/anchor/orient.json">
  <front>
    <title>RAPP/1 specification anchor (append-only chain of record)</title>
    <author >
      <organization></organization>
    </author>
    <date year="2026"/>
  </front>
</reference>


    </references>

</references>


<?line 1028?>

<section anchor="acknowledgements"><name>Acknowledgements</name>

<t>The specification was hardened through adversarial review rounds recorded in its
revision log. The chain of record and its verifier are at <xref target="RAPP1-ANCHOR"/>.</t>

</section>


  </back>

<!-- ##markdown-source:


-->

</rfc>

