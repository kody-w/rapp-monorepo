name: Electron Desktop

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  desktop:
    name: ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-14, windows-latest]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: |
            typescript/package-lock.json
            typescript/ui/package-lock.json
            typescript/desktop/package-lock.json

      - name: Install and build OpenRappter core
        working-directory: typescript
        run: |
          npm ci
          npm run build

      - name: Install Electron desktop runtime
        working-directory: typescript/desktop
        run: npm ci

      - name: Compile and test desktop security and release contracts
        working-directory: typescript/desktop
        run: |
          npm run build
          npm test

      - name: Exercise Apple Silicon Omarchy gateway integration
        if: runner.os == 'macOS'
        working-directory: typescript
        run: npx vitest run src/gateway/__tests__/vm-gateway.test.ts

      - name: Audit desktop production dependencies
        working-directory: typescript/desktop
        run: npm audit --audit-level=high

      - name: Run real Electron smoke on Linux
        if: runner.os == 'Linux'
        working-directory: typescript/desktop
        run: |
          sudo chown root:root node_modules/electron/dist/chrome-sandbox
          sudo chmod 4755 node_modules/electron/dist/chrome-sandbox
          xvfb-run -a env OPENRAPPTER_DESKTOP_SMOKE=1 ./node_modules/.bin/electron .

      - name: Run real Electron smoke on macOS
        if: runner.os == 'macOS'
        working-directory: typescript/desktop
        run: OPENRAPPTER_DESKTOP_SMOKE=1 ./node_modules/.bin/electron .

      - name: Run real Electron smoke on Windows
        if: runner.os == 'Windows'
        timeout-minutes: 5
        working-directory: typescript/desktop
        shell: pwsh
        run: |
          $env:OPENRAPPTER_DESKTOP_SMOKE = "1"
          # The 120s this step used to set is now the product default (#223), so
          # CI and a real user get the same budget -- which is the point: the
          # runner being slow is a fair proxy for a loaded user machine, and a
          # value only CI enjoys would have hidden the problem again.
          .\node_modules\.bin\electron.cmd .

      - name: Build macOS DMG without publishing
        if: runner.os == 'macOS'
        working-directory: typescript/desktop
        run: CSC_IDENTITY_AUTO_DISCOVERY=false npm run dist -- --mac dmg --config.mac.notarize=false

      - name: Verify RAPP Work DMG and generate release metadata
        if: runner.os == 'macOS'
        working-directory: typescript/desktop
        run: npm run release:manifest -- --require-clean

      - name: Upload RAPP Work macOS artifacts
        if: runner.os == 'macOS'
        uses: actions/upload-artifact@v4
        with:
          name: rapp-work-macos-${{ github.sha }}
          path: |
            typescript/desktop/dist/*.dmg
            typescript/desktop/dist/*.dmg.sha256
            typescript/desktop/dist/*.dmg.manifest.json
          if-no-files-found: error
