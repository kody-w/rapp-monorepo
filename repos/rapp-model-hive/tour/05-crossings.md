# 5. Crossings

To show one person's message in another person's app, the Hive goes forward through the sender's lens into the shared view, then back through the newest lens version that rebuilds the receiver's schema exactly. The result is an unsigned proposal: only the receiver can sign it.

### Blake's phone task, as Avery's laptop app would read it

Refused (REFUSE_CROSSING): No lens version expresses this message in the target schema exactly; the Hive will not invent missing fields.

### Avery's laptop task, as Blake's phone app would read it

Crossed. Could not be expressed in the target: due. Payload: `{"assignee": "avery", "id": "T-100", "operation": "task", "profile": "contoso-tasks/1", "text": "Draft the onboarding checklist"}`

### Casey's tablet task, as Avery's laptop app would read it

Crossed. Could not be expressed in the target: nothing. Payload: `{"due": "2026-09-27", "operation": "task", "owner": "casey", "profile": "contoso-tasks/1", "task_id": "T-105", "title": "Sketch the welcome poster"}`

Try it: `cd vendor && python3 -B -m rapp_hive2 cross ../model/hive <8+ hex of a message> avery-laptop`.

## Reads
- Content frames and every lens version that was ever active.

## Does
Shows crossings between members' schemas.

## Writes
Nothing: this room is generated by `tools/build.py` from the signed frames.

## Human check
A crossing is a proposal; the receiver decides whether to sign it.
