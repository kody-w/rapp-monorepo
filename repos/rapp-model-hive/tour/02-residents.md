# 2. Residents

Policy v2 is active: every member decides, and joining needs 2 grants and a key confirmed by a person.

| Member | How they got in |
|---|---|
| Avery (team lead, laptop) | founder (the rapp-hive/1 owner, now steward) |
| Blake (field engineer, phone) | admitted by grants |
| Casey (designer, tablet) | admitted by grants |
| Drew (new analyst, desktop) | admitted by grants |
| Emery (front-desk kiosk, joined on the old system) | joined on the old system; admitted under the first policy's rules |

| Waiting | Progress |
|---|---|
| Frankie (contractor, still waiting) | 1 of 2 grants, key confirmed |

Emery asked to join on the old system, before the Hive moved to rapp-hive/2. Policy v2 keeps pending requests on the rules they were made under (`keep-pinned`), and under policy v1 only Avery, the steward, decides. So Blake's approval did not count (1 grant refused as `REFUSE_NOT_DECIDER`), and Avery's one approval was enough. Nobody overrode anything, and no request was stranded.

Frankie's task waits in quarantine (1 message) until Frankie is admitted. It is kept, not deleted. Because Frankie is not a member yet, its extra `invoice` field teaches the Hive nothing.

## Reads
- `hive2.accept`, `hive2.join`, `hive2.grant` and `hive2.attest` frames.

## Does
Lists members and waiting requests.

## Writes
Nothing: this room is generated by `tools/build.py` from the signed frames.

## Human check
Confirm each key fingerprint with its owner by voice, video or in person before granting.
