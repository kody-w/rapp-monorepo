{
  "claimsGrantedOnPass": [
    "The adapter can preserve an HTTPS GitHub repository locator and exact commit without broadening either value."
  ],
  "claimsNeverGranted": [
    "Authority over a Hive, organization, protocol, or ecosystem.",
    "Semantic compatibility beyond exact repository-locator preservation.",
    "Permission to execute retrieved content."
  ],
  "conformanceId": "urn:hive-hub:conformance:github-repository-locator:1",
  "kind": "conformance-contract",
  "protocolId": "github-repository-v1",
  "requirements": [
    {
      "id": "exact-commit",
      "rule": "The revision MUST be exactly 40 lowercase hexadecimal characters."
    },
    {
      "id": "same-repository",
      "rule": "Browse, archive, and raw URLs MUST retain the declared owner and repository."
    },
    {
      "id": "no-ref-expansion",
      "rule": "The adapter MUST NOT replace the exact commit with a branch, tag, default branch, or moving reference."
    },
    {
      "id": "locator-only",
      "rule": "The adapter MUST expose locator semantics only and MUST NOT add authority or compatibility claims."
    },
    {
      "id": "inert-content",
      "rule": "The adapter MUST keep retrieved content inert pending separate approval and verification."
    }
  ],
  "version": "1.0.0"
}
